// RHCE Ansible Practice: Content Collections, Execution Environments & ansible-navigator
// Fills a confirmed gap: across all 4 live "ansible" exam_type papers,
// ansible-navigator / Execution Environments / Content Collections appear
// in only 1 question total, despite "Automation Content Navigator" being a
// named topic area on the current EX294 (RHCE, RHEL 9-based) exam blueprint
// (confirmed via web search 2026-09-04). This paper goes deep on exactly
// that gap: FQCNs and ansible-galaxy collection installation, what an
// Execution Environment is and why it replaces the bare-metal control node
// model, and ansible-navigator's actual subcommands/settings precedence/
// run modes.
//
// VERIFIED FINDINGS (web search against docs.ansible.com's Navigator,
// Builder and Runner project docs, 2026-09-04 — pretrained knowledge on
// this tooling is treated as potentially stale per this build's brief):
//   - ansible-navigator's real subcommands (docs.ansible.com/projects/
//     navigator/subcommands/) are: welcome, run, exec, images, doc, config,
//     collections, builder, replay, inventory, lint, settings. There is NO
//     "doctor" subcommand in the current documented CLI — a "doctor"-named
//     command does not exist; this paper does not test it as real, and one
//     question below directly tests recognizing it as fabricated.
//   - `ansible-navigator images` explores/inspects execution environment
//     container images (name, tags, contents) rather than running a
//     playbook.
//   - `ansible-navigator run <playbook>` supports `--mode interactive`
//     (TUI, browse results/drill into tasks, default) and `--mode stdout`
//     (prints like plain `ansible-playbook`, suited to CI/CD).
//   - Settings precedence (lowest to highest): built-in defaults -> settings
//     file (./ansible-navigator.yml, then ~/.ansible-navigator.yml, or the
//     path in ANSIBLE_NAVIGATOR_CONFIG) -> environment variables -> CLI
//     parameters. CLI flags always win.
//   - `--pull-policy` (`--pp`) values: always, missing, never, tag (pull if
//     tag is 'latest', else only if not present locally).
//   - Execution Environments (docs.ansible.com/projects/runner /
//     projects/builder): an EE is an OCI-compliant container image bundling
//     ansible-core, Content Collections, and their Python/system
//     dependencies — a portable, versioned control-node runtime, replacing
//     "install everything on the control node by hand."
//   - ansible-builder builds EEs from a definition YAML with 7 top-level
//     sections: version, images, dependencies, additional_build_files,
//     additional_build_steps, build_arg_defaults, options.
//   - FQCN format is namespace.collection.content (3 dot-separated parts),
//     e.g. ansible.posix.firewalld or community.general.ufw.
//   - `ansible-galaxy collection install -r requirements.yml` installs
//     collections listed in a requirements file (roles need a separate
//     `ansible-galaxy role install -r requirements.yml` — one requirements
//     file can list both, but each is installed via its own command);
//     default install path is the first entry in COLLECTIONS_PATHS
//     (~/.ansible/collections by default) unless -p overrides it.
//
// SOURCE: authored from docs.ansible.com's Navigator/Builder/Runner project
// documentation and Red Hat's Ansible Automation Platform docs, cross-
// checked against the current publicly-described EX294 exam objectives;
// verified via web search 2026-09-04. Not copied from any official Red Hat
// exam material.

export const HEADER_LABEL = "RHCE Ansible Practice: Content Collections, Execution Environments & ansible-navigator";

export const PAPER = {
  slug: "rhce-navigator-collections-ee",
  title: "RHCE Ansible Practice: Content Collections, Execution Environments & ansible-navigator",
  examType: "ansible",
  focusTags: ["RHCE", "EX294", "ansible-navigator", "Execution Environments", "Content Collections", "ansible-builder"],
  questionCount: 38,
  difficulty: "advanced",
  sortOrder: 5,
  timeLimitMinutes: 60,
  positiveMarks: 1,
  negativeMarks: 0,
};

const SRC_COLLECTIONS = "Ansible Content Collections — FQCNs, ansible-galaxy collection install, requirements.yml — authored from docs.ansible.com's collections user guide; verified via web search 2026-09-04.";
const SRC_EE = "Execution Environments — ansible-builder/ansible-runner definitions and workflow — authored from docs.ansible.com's Builder and Runner project documentation; verified via web search 2026-09-04.";
const SRC_NAVIGATOR = "ansible-navigator usage — subcommands, settings precedence, run modes, pull policy — authored from docs.ansible.com/projects/navigator documentation; verified via web search 2026-09-04. Confirmed no 'doctor' subcommand exists in the current documented CLI.";

export const SECTIONS = [
  {
    name: "Content Collections & ansible-galaxy",
    subject: "Ansible",
    sourceNote: SRC_COLLECTIONS,
    questions: [
      { topic: "FQCN Format", stem: "A Fully Qualified Collection Name (FQCN) used to reference a module in a playbook has the form:", options: ["module.namespace.collection", "namespace.collection.content (e.g. ansible.posix.firewalld)", "collection.namespace.module", "namespace/collection/module"], correct: 1, explanation: "An FQCN is three dot-separated parts: namespace.collection.content — e.g. ansible.posix.firewalld or community.general.ufw.", },
      { topic: "Why Use FQCNs", stem: "Why does Red Hat's RHCE curriculum and Ansible's own best-practice guidance recommend always using FQCNs for module names in playbooks, rather than short names like `firewalld`?", options: ["Short names are always rejected by ansible-lint with no way to allow them", "FQCNs remove ambiguity when multiple installed collections ship a module with the same short name, and make the playbook's dependency on a specific collection explicit", "FQCNs run faster than short module names at task execution time", "Short names only work inside roles, never in top-level playbooks"], correct: 1, explanation: "Multiple collections can ship modules with identical short names; the FQCN disambiguates which collection's implementation is used and documents the collection dependency directly in the playbook.", },
      { topic: "ansible-galaxy collection install", stem: "What does `ansible-galaxy collection install community.general` do if no version or path is specified?", options: ["Installs a fixed, hardcoded old version to the current directory", "Installs the latest available version of the collection to the first path listed in COLLECTIONS_PATHS (by default, ~/.ansible/collections)", "Fails immediately, since a version must always be specified", "Installs every collection published under the community.* namespace"], correct: 1, explanation: "Without a version constraint, ansible-galaxy installs the latest available version; without -p, it installs to the first entry in COLLECTIONS_PATHS, which defaults to ~/.ansible/collections.", },
      { topic: "Version Pinning", stem: "In `ansible-galaxy collection install`, how is a specific version range requested for a collection?", options: ["By appending a version specifier, e.g. `community.general:>=5.0.0,<6.0.0`", "Version ranges are not supported; only exact single versions can be installed", "By setting ANSIBLE_COLLECTIONS_VERSION as an environment variable", "By editing ansible.cfg's [defaults] section only"], correct: 0, explanation: "ansible-galaxy collection install accepts a version range identifier appended to the collection name (e.g. name:>=X,<Y), and production use is generally recommended to pin versions this way.", },
      { topic: "requirements.yml Structure", stem: "A single requirements.yml file lists both `roles:` and `collections:` entries. What is true about installing from that one file?", options: ["A single `ansible-galaxy install -r requirements.yml` command installs both roles and collections together automatically", "Roles and collections must be installed with separate commands — `ansible-galaxy role install -r requirements.yml` installs only the roles, `ansible-galaxy collection install -r requirements.yml` installs only the collections", "Only collections can be listed in requirements.yml; roles require a completely separate file format", "requirements.yml can only reference content already present locally, never remote sources"], correct: 1, explanation: "Even when both are listed in one requirements.yml, roles and collections are installed via their own respective ansible-galaxy subcommands pointed at the same file.", },
      { topic: "Collection Sources", stem: "Besides Ansible Galaxy itself, which of the following are valid sources ansible-galaxy can install a collection from?", options: ["Only Ansible Galaxy — no other source is ever supported", "Git repositories, direct URLs to a tarball, and local tarball files, in addition to Galaxy/Automation Hub", "Only a private Automation Hub instance, never Galaxy or Git", "Only local filesystem paths, never any network source"], correct: 1, explanation: "ansible-galaxy collection install supports Galaxy/Automation Hub, Git repositories, direct URLs, and local tarballs, giving flexibility for both connected and air-gapped/offline environments.", },
      { topic: "Collection Install Path", stem: "Running `ansible-galaxy collection install -r requirements.yml -p ./collections` does what differently from omitting `-p`?", options: ["It installs the collections into the project-local ./collections directory instead of the default COLLECTIONS_PATHS location", "It validates the requirements.yml syntax without actually installing anything", "It installs collections only for the current user's default role path", "The -p flag has no effect on collection installs, only on role installs"], correct: 0, explanation: "-p overrides the install destination, letting a project vendor its collections into a local, project-relative directory rather than the shared default path.", },
      { topic: "Collections vs. Roles", stem: "What is the key structural difference between an Ansible Collection and an Ansible Role?", options: ["A Role can only contain tasks, while a Collection can only contain variables", "A Collection is a distributable package that can bundle multiple roles, modules, plugins, and playbooks together under one namespace; a Role is a single reusable unit of tasks/handlers/templates typically bundled inside (or alongside) a collection", "Roles and Collections are two names for exactly the same artifact with no structural difference", "A Collection cannot be published to Galaxy, only Roles can"], correct: 1, explanation: "A Collection is the broader packaging format — it can contain modules, plugins, playbooks, and one or more roles; a Role is a narrower, single reusable automation unit that a collection commonly ships inside.", },
      { topic: "Discovering Collection Content", stem: "Which command lets you browse the modules/plugins available inside collections already installed on the current control node?", options: ["ansible-galaxy collection list, plus ansible-doc <fqcn> for details on a specific module", "There is no way to enumerate installed collection content; it must be looked up in online docs only", "ansible-inventory --list-collections", "ansible-config dump --collections"], correct: 0, explanation: "ansible-galaxy collection list enumerates installed collections and their versions; ansible-doc <FQCN> shows documentation for a specific module/plugin inside one.", },
    ],
  },
  {
    name: "Execution Environments (ansible-builder / ansible-runner)",
    subject: "Ansible",
    sourceNote: SRC_EE,
    questions: [
      { topic: "What an Execution Environment Is", stem: "What is an Ansible Execution Environment (EE)?", options: ["A Python virtual environment created by pip on the control node", "An OCI-compliant container image that bundles ansible-core, Ansible Content Collections, and their Python/system-level dependencies into one portable, versioned control-node runtime", "A dedicated managed node used only for testing playbooks before production", "A synonym for an Ansible inventory group"], correct: 1, explanation: "An Execution Environment is a container image (OCI-compliant) that packages ansible-core plus the collections and dependencies a set of playbooks needs, so the automation runs the same way regardless of what's installed on the host running it.", },
      { topic: "Why Execution Environments Exist", stem: "What problem do Execution Environments solve compared to the traditional model of installing Ansible and all its collection dependencies directly on a bare-metal/VM control node?", options: ["They make playbooks run without needing an inventory file", "They eliminate 'it works on my control node' dependency drift — every run uses the exact same pinned Ansible version, collections, and Python/system packages, packaged once and reused anywhere a container runtime is available", "They remove the need for SSH connectivity to managed nodes entirely", "They allow playbooks to run without any Python interpreter on managed nodes"], correct: 1, explanation: "EEs solve dependency drift and 'works on my machine' problems: the exact same container image (with pinned versions) can run identically in a developer's environment, CI, and Automation Controller.", },
      { topic: "ansible-builder's Role", stem: "What does ansible-builder do?", options: ["It runs playbooks inside a container", "It builds Execution Environment container images from a YAML definition file describing dependencies (collections, Python packages, system packages)", "It converts YAML playbooks into JSON", "It manages Ansible Vault-encrypted secrets"], correct: 1, explanation: "ansible-builder is the tool that constructs an EE image from an execution-environment definition file, resolving and layering in the specified collections, Python requirements, and system packages.", },
      { topic: "EE Definition File Sections", stem: "An ansible-builder execution-environment definition file's `dependencies` section is used to specify:", options: ["Only the base container image to build from", "What to install into the image — ansible-core/ansible-runner, Python packages, system packages, and Ansible Collections", "Only which playbooks are allowed to run inside the resulting image", "The inventory sources the EE is permitted to use"], correct: 1, explanation: "The dependencies section lists what gets installed into the built image: ansible-core/runner, Python requirements, system-level packages, and collections.", },
      { topic: "EE Definition File Sections (2)", stem: "Which ansible-builder definition file section lets you specify default values for build arguments passed into the underlying container build process?", options: ["additional_build_files", "build_arg_defaults", "options", "images"], correct: 1, explanation: "build_arg_defaults specifies default values for build args as a dictionary, used during the underlying container image construction.", },
      { topic: "ansible-runner's Role", stem: "What is ansible-runner's role in the Execution Environment workflow?", options: ["It builds the EE image from a definition file, the same job as ansible-builder", "It runs Ansible content (playbooks/modules/roles) inside a container using a given Execution Environment image, handling the container invocation so content executes as expected", "It only manages Ansible Galaxy authentication tokens", "It replaces ansible-navigator's interactive TUI entirely"], correct: 1, explanation: "ansible-runner executes Ansible content inside the container runtime using a specified EE image — it's the execution layer, while ansible-builder is the image-construction layer.", },
      { topic: "Base Image Selection", stem: "In an EE definition file, which top-level section specifies the base container image the new Execution Environment is built on top of?", options: ["dependencies", "images", "additional_build_steps", "version"], correct: 1, explanation: "The images section specifies the base parent container image for the execution environment build.", },
      { topic: "Adding Custom Build Logic", stem: "If a team needs to run a custom shell command (e.g. installing an internal CA certificate) partway through the EE image build, which definition file section is designed for that?", options: ["options", "additional_build_steps", "build_arg_defaults", "dependencies"], correct: 1, explanation: "additional_build_steps lets you insert custom build commands at defined stages (e.g. prepend/append) of the image creation process.", },
      { topic: "EE Portability", stem: "A playbook developed and tested against a given Execution Environment image is handed to a CI pipeline and, separately, to Automation Controller. What must be true for the playbook to behave identically in all three places?", options: ["Nothing — EEs guarantee identical behavior automatically regardless of anything else", "All three must pull and run the exact same EE image (same tag/digest); if they pull different versions of the image, dependency versions can differ between runs", "Only the CI pipeline needs the EE; Automation Controller ignores EE images", "The playbook must be rewritten separately for each execution context"], correct: 1, explanation: "EEs only guarantee consistency if the same image (same tag or, more strictly, the same digest) is actually used everywhere — a 'latest'-tag pull at different times can still drift.", },
      { topic: "Traditional Control Node vs. EE", stem: "Before Execution Environments, what did a team typically have to do to keep a bare-metal Ansible control node's collection/Python dependencies consistent across multiple team members' machines?", options: ["Nothing was needed; ansible-core has no external dependencies", "Manually replicate the same pip/ansible-galaxy installs, versions, and OS packages on every machine (or via config management), which was error-prone and easy to let drift", "Use Ansible Vault to synchronize dependencies automatically", "Install Docker on every managed node"], correct: 1, explanation: "Without EEs, keeping a control node's dependency set consistent across machines required manual or configuration-managed replication of the same installs — exactly the drift problem EEs are designed to remove.", },
    ],
  },
  {
    name: "ansible-navigator Usage",
    subject: "Ansible",
    sourceNote: SRC_NAVIGATOR,
    questions: [
      { topic: "ansible-navigator Purpose", stem: "What is ansible-navigator?", options: ["A replacement for ansible-galaxy that only manages collections", "A text-based user interface (TUI) and CLI tool for running and reviewing Ansible content, designed around Execution Environments as its default runtime", "A GUI-only desktop application with no command-line interface", "A linting tool that only checks YAML syntax"], correct: 1, explanation: "ansible-navigator is Ansible's TUI/CLI front end for running playbooks and exploring related content (inventory, docs, config), built with Execution Environment-based execution as its default model.", },
      { topic: "ansible-navigator run", stem: "Which ansible-navigator subcommand actually executes a playbook?", options: ["exec", "run", "replay", "lint"], correct: 1, explanation: "`ansible-navigator run <playbook>` is the subcommand that runs a playbook (inside an Execution Environment by default).", },
      { topic: "ansible-navigator images", stem: "What does the `ansible-navigator images` subcommand do?", options: ["Runs a playbook using the smallest available image", "Explores available Execution Environment container images — inspecting details like image contents, versions, and installed collections", "Builds a brand-new EE image from a definition file", "Uploads a container image to a registry"], correct: 1, explanation: "images lets you explore/inspect Execution Environment images already available locally or configured for use — not build or run anything.", },
      { topic: "ansible-navigator builder", stem: "What does the `ansible-navigator builder` subcommand do?", options: ["It is an alias with no functionality of its own", "It builds an Execution Environment (container image), integrating ansible-builder's functionality into the navigator CLI", "It only validates a definition YAML file's syntax without building", "It lists all builders registered with a container runtime"], correct: 1, explanation: "The builder subcommand's documented purpose is to build an Execution Environment (container image) — surfacing ansible-builder-style functionality directly through ansible-navigator.", },
      { topic: "ansible-navigator collections", stem: "What does the `ansible-navigator collections` subcommand let you do?", options: ["Install new collections from Galaxy directly", "Explore the collections available inside the current Execution Environment (or configured collection paths)", "Delete unused collections to free disk space", "Publish a collection to Automation Hub"], correct: 1, explanation: "collections is an exploration subcommand — it lets you browse what collections are available/installed in the context ansible-navigator is using, not install or manage them.", },
      { topic: "Nonexistent Subcommand Check", stem: "A colleague claims RHCE candidates should memorize `ansible-navigator doctor` for diagnosing execution-environment problems. Based on the currently documented ansible-navigator subcommand list, is this accurate?", options: ["Yes, `doctor` is a core subcommand documented alongside `run` and `images`", "No — the documented subcommand list (welcome, run, exec, images, doc, config, collections, builder, replay, inventory, lint, settings) does not include a `doctor` subcommand; this is not a real ansible-navigator command", "Yes, but only when running inside Automation Controller", "No, because `doctor` was renamed to `exec` in recent releases"], correct: 1, explanation: "There is no `doctor` subcommand in ansible-navigator's documented CLI. The real subcommand set (per docs.ansible.com/projects/navigator/subcommands/) is welcome, run, exec, images, doc, config, collections, builder, replay, inventory, lint, and settings — 'doctor' does not appear among them.", },
      { topic: "Interactive vs. stdout Mode", stem: "Running `ansible-navigator run site.yml --mode stdout` instead of the default interactive mode changes what?", options: ["It skips executing the playbook entirely and only validates syntax", "Output is printed to the terminal much like plain `ansible-playbook`, instead of launching the browsable TUI — useful for CI/CD pipelines where there's no interactive terminal", "It forces the playbook to run without an Execution Environment", "It disables all logging for the run"], correct: 1, explanation: "--mode stdout prints results directly to the terminal (similar to ansible-playbook's own output) rather than opening the interactive TUI, which is why it's the mode typically used in CI/CD.", },
      { topic: "Settings Precedence", stem: "For a given ansible-navigator setting available via all three mechanisms, what is the correct precedence order from LOWEST to HIGHEST priority?", options: ["CLI parameters, environment variables, settings file, built-in defaults", "Built-in defaults, settings file, environment variables, CLI parameters", "Environment variables, built-in defaults, CLI parameters, settings file", "Settings file, CLI parameters, built-in defaults, environment variables"], correct: 1, explanation: "Precedence from lowest to highest is: built-in defaults -> settings file -> environment variables -> CLI parameters, with CLI flags always winning when a setting is specified in more than one place.", },
      { topic: "Settings File Discovery", stem: "If ANSIBLE_NAVIGATOR_CONFIG is not set, where does ansible-navigator look for a settings file, and in what order?", options: ["Only in /etc/ansible-navigator.yml, nowhere else", "It checks the current project directory (./ansible-navigator.<ext>) before falling back to the home directory (~/.ansible-navigator.<ext>)", "Only in the home directory; the current directory is never checked", "It never uses a settings file unless ANSIBLE_NAVIGATOR_CONFIG is explicitly set"], correct: 1, explanation: "Absent an explicit ANSIBLE_NAVIGATOR_CONFIG path, ansible-navigator checks the current project directory first, then the home directory — the project-directory file takes precedence over the home-directory one.", },
      { topic: "Pull Policy", stem: "What does `--pull-policy missing` (short form `--pp missing`) do when ansible-navigator needs an Execution Environment image?", options: ["Always re-pulls the image from the registry regardless of whether it's already present locally", "Only pulls the image if it is not already available locally, avoiding an unnecessary re-pull of an image that's already present", "Never pulls the image under any circumstance, even if missing, causing the run to fail", "Pulls a different, smaller fallback image if the requested one is missing"], correct: 1, explanation: "The `missing` pull policy only triggers a pull when the image isn't already present locally, distinct from `always` (always re-pull), `never` (never pull), and `tag` (re-pull only if the tag is 'latest').", },
      { topic: "ansible-navigator config", stem: "What does the `ansible-navigator config` subcommand let you review?", options: ["The current ansible-navigator settings only, never ansible.cfg values", "The current Ansible configuration (equivalent in spirit to `ansible-config`, but explorable via navigator's interface)", "Only Execution Environment build configuration", "Kubernetes cluster configuration for K8s-targeted playbooks"], correct: 1, explanation: "config lets you explore the current Ansible configuration (the ansible.cfg-driven settings), distinct from `settings`, which reviews ansible-navigator's own settings.", },
      { topic: "ansible-navigator settings vs. config", stem: "What is the distinction between the `ansible-navigator settings` subcommand and the `ansible-navigator config` subcommand?", options: ["They are exact synonyms with no functional difference", "`settings` reviews ansible-navigator's own current settings (from defaults/settings file/env vars/CLI); `config` reviews the underlying Ansible configuration (ansible.cfg-driven)", "`settings` only works in interactive mode; `config` only works in stdout mode", "`config` is deprecated in favor of `settings` and no longer functions"], correct: 1, explanation: "settings surfaces ansible-navigator's own configuration state; config surfaces the separate, underlying Ansible (ansible.cfg) configuration — two different configuration layers.", },
      { topic: "ansible-navigator doc", stem: "What does `ansible-navigator doc <fqcn>` do?", options: ["Deletes the documentation cache for that module", "Reviews the documentation for the named module or plugin, similar in purpose to the standalone `ansible-doc` command but through navigator's interface", "Publishes documentation to Ansible Galaxy", "Only works for modules in the ansible.builtin collection"], correct: 1, explanation: "doc reviews documentation for a module or plugin — the navigator-integrated equivalent of running `ansible-doc` directly.", },
      { topic: "ansible-navigator exec", stem: "How does `ansible-navigator exec` differ from `ansible-navigator run`?", options: ["exec runs an arbitrary command inside the configured Execution Environment container, rather than running an Ansible playbook specifically", "exec and run are identical aliases", "exec only works with Kubernetes-based Execution Environments", "exec is used exclusively to execute ansible-galaxy commands"], correct: 0, explanation: "exec runs a given command within an Execution Environment (useful for debugging what's actually inside the EE, e.g. checking installed packages), whereas run specifically executes a playbook.", },
      { topic: "ansible-navigator inventory", stem: "What is the purpose of the `ansible-navigator inventory` subcommand?", options: ["To install new managed nodes automatically", "To explore an inventory — browsing groups, hosts, and variables interactively", "To convert a YAML inventory into an INI-format inventory", "To validate playbook syntax against inventory hostnames"], correct: 1, explanation: "inventory lets you explore an inventory's structure (groups, hosts, variables) interactively, useful for verifying inventory content before a run.", },
      { topic: "ansible-navigator replay", stem: "What does `ansible-navigator replay` let you do?", options: ["Re-run a playbook from scratch against live infrastructure", "Explore a previous run using its saved playbook artifact, without re-executing anything against real hosts", "Replay a playbook's Jinja2 templates step-by-step in a debugger", "Restore a previous Execution Environment image version"], correct: 1, explanation: "replay opens a previous run's saved playbook artifact for exploration in the TUI, letting you review what happened without touching real infrastructure again.", },
      { topic: "ansible-navigator lint", stem: "What does the `ansible-navigator lint` subcommand do?", options: ["It only checks whether an Execution Environment image is signed", "It lints a file or directory for common errors and issues, similar in spirit to running ansible-lint directly, through navigator's interface", "It compiles a playbook into a binary artifact", "It is a synonym for `ansible-navigator run --check`"], correct: 1, explanation: "lint checks a file or directory for common errors/issues (an ansible-lint-style pass) surfaced through the navigator CLI/TUI.", },
      { topic: "Why EE-First Matters for Navigator", stem: "Why is ansible-navigator designed around Execution Environments as its default execution model, rather than running directly against whatever is installed on the local control node?", options: ["Because ansible-navigator cannot run playbooks without a container runtime under any configuration", "Because defaulting to EE-based execution means the collections/dependencies used for a run are the pinned, portable set baked into the image, rather than depending on whatever happens to be installed locally — directly addressing the dependency-drift problem EEs exist to solve", "Because EEs make ansible-navigator run without needing an inventory", "Because Red Hat requires all Ansible tooling to run exclusively in containers as of RHEL 9"], correct: 1, explanation: "ansible-navigator's EE-first design ties directly back to why EEs exist: it makes the exact dependency set explicit and portable, rather than implicitly depending on the local control node's ad hoc installs (navigator does support a --execution-environment false local-execution mode, but EE-based execution is the intended default).", },
      { topic: "Content Collections in Navigator's doc View", stem: "When running `ansible-navigator doc ansible.posix.firewalld`, where does navigator resolve that module's documentation from?", options: ["From a hardcoded internal list of module names that ships with navigator itself", "From the ansible.posix collection as installed/available in the currently configured Execution Environment (or local install path if running without an EE)", "It always fails, since navigator cannot resolve any FQCN outside ansible.builtin", "From a live network call to Ansible Galaxy's website on every invocation"], correct: 1, explanation: "doc resolves a module/plugin's documentation from wherever that collection actually lives for the current run context — the configured EE image's installed collections, or the local install path when running without an EE.", },
    ],
  },
];
