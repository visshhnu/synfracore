// Terraform Associate Practice: HCP Terraform Workspaces & Projects
// Fills a confirmed gap vs. the current HashiCorp Terraform Associate (004)
// exam (tests Terraform 1.12; 003 was retired 2026-01-08): a live DB query
// against the 4 existing "terraform" exam_type papers (2026-09-04) found
// only 4 questions total, across 2 papers, on HCP Terraform workspaces —
// terraform-associate-modules-state-management Q17-18 cover CLI workspaces
// (a distinct, unrelated CLI-only feature) and terraform-associate-
// maintain-hcp-terraform Q6-13 cover HCP Terraform only at an overview
// level (what it is, what a workspace/project/variable set/Sentinel IS,
// the CLI-driven login flow, the general benefit of remote execution) —
// with zero coverage of workspace WORKFLOW TYPES (VCS/CLI/API-driven),
// execution MODES (remote/local/agent), the exact run STATE sequence, run
// triggers, or how policy sets actually ATTACH to workspaces/projects/tags
// with enforcement levels. This paper deliberately does not repeat that
// existing paper's Sentinel-definition or import-block content — it goes
// deep on workspace/project organization and the run lifecycle instead, a
// different angle on the same domain.
//
// Facts below verified via web search against developer.hashicorp.com
// (2026-09-04), not from training-data memory:
//  - Three workflow types: VCS-driven (webhook-triggered from a linked
//    repo, includes automatic speculative plans on pull requests), CLI-driven
//    (practitioner runs `terraform plan`/`apply` locally via `terraform login`
//    plus a `cloud` block, but execution itself happens remotely in HCP
//    Terraform), and API-driven (a run is created directly via the HCP
//    Terraform API — the most flexible but requires building your own
//    tooling/automation around it). Source: developer.hashicorp.com/terraform/
//    cloud-docs/workspaces/run/ui, .../run/cli, and hashicorp.com/blog/
//    which-terraform-workflow-should-i-use-vcs-cli-or-api.
//  - Execution modes (a workspace SETTING, distinct from workflow type):
//    Remote (HCP Terraform runs on its own disposable VMs — required for
//    Sentinel/OPA policy checks, cost estimation, etc.), Local (state is
//    still stored in HCP Terraform, but the run executes on the
//    practitioner's own machine via the CLI-driven workflow; HCP Terraform
//    does NOT evaluate workspace variables or variable sets in local
//    execution mode), and Agent (runs execute via a self-hosted HCP
//    Terraform Agent for isolated/private-network infrastructure). Source:
//    developer.hashicorp.com/terraform/cloud-docs/workspaces/settings.
//  - Projects: every workspace belongs to exactly one project; every
//    organization gets an auto-created "Default Project" (can be renamed,
//    cannot be deleted); HCP Terraform's project structure is flat — there
//    is no project nesting/parent-child inheritance. Moving a workspace to
//    a different project can change who can read/write it. Source:
//    developer.hashicorp.com/terraform/cloud-docs/projects and .../tutorials/
//    cloud/projects.
//  - Run states/stages sequence: pending -> fetching (if needed) -> plan ->
//    (OPA policy check, if enabled) -> cost estimation (if enabled) ->
//    (Sentinel policy check, if enabled) -> confirm/auto-apply -> apply ->
//    completion (applied / planned and finished / errored / discarded /
//    canceled) — OPA checks run before cost estimation, Sentinel checks run
//    after cost estimation. A run first in the queue proceeds automatically
//    to the plan stage; while a plan is unconfirmed/undiscarded, no other
//    run in that workspace can start. Source: developer.hashicorp.com/
//    terraform/cloud-docs/workspaces/run/states and .../run/ui.
//  - CLI-driven auto-apply requires an explicit -auto-approve flag on
//    `terraform apply`; UI- and API-driven runs instead respect the
//    workspace's own auto-apply setting. Source: .../run/cli and .../run/ui.
//  - Policy sets (Sentinel or OPA) can be attached at the workspace,
//    project, workspaces-matching-a-tag, or whole-organization (global)
//    level, with three enforcement levels: advisory (never blocks the run,
//    informational only), mandatory overridable / soft-mandatory (blocks
//    unless a sufficiently-permissioned user overrides it), and hard-
//    mandatory (blocks the run outright; cannot be overridden). Source:
//    developer.hashicorp.com/terraform/cloud-docs/workspaces/policy-
//    enforcement and .../workspaces/policy-enforcement/manage-policy-sets.
// Current Terraform Associate 004 exam guide (per developer.hashicorp.com/
// terraform/tutorials/certification-004/associate-review-004 as of this
// build) explicitly names HCP Terraform, workspace types, VCS-driven runs,
// the run lifecycle, and Sentinel/policy as tested topics.

export const HEADER_LABEL = "Terraform Associate Practice: HCP Terraform Workspaces & Projects";

export const PAPER = {
  slug: "terraform-hcp-workspaces-projects",
  title: "Terraform Associate Practice: HCP Terraform Workspaces & Projects",
  examType: "terraform",
  focusTags: ["Terraform Associate", "HCP Terraform", "Workspaces", "Projects"],
  questionCount: 38,
  difficulty: "advanced",
  sortOrder: 6,
  timeLimitMinutes: 60,
  positiveMarks: 1,
  negativeMarks: 0,
};

const SRC_WORKFLOWS =
  "Workspace Workflow Types & Execution Modes — authored from developer.hashicorp.com/terraform/cloud-docs/workspaces/run/ui, .../run/cli, .../workspaces/settings, and hashicorp.com/blog/which-terraform-workflow-should-i-use-vcs-cli-or-api; verified via web search against current HCP Terraform docs, not copied from any official HashiCorp exam material.";
const SRC_PROJECTS =
  "Projects — Organizing Workspaces — authored from developer.hashicorp.com/terraform/cloud-docs/projects and .../tutorials/cloud/projects; verified via web search against current HCP Terraform docs, not copied from any official HashiCorp exam material.";
const SRC_RUN_LIFECYCLE =
  "Run Lifecycle — authored from developer.hashicorp.com/terraform/cloud-docs/workspaces/run/states and .../run/ui; verified via web search against current HCP Terraform docs, not copied from any official HashiCorp exam material.";
const SRC_POLICY =
  "Policy Sets: Attaching Sentinel/OPA at Scale — authored from developer.hashicorp.com/terraform/cloud-docs/workspaces/policy-enforcement and .../workspaces/policy-enforcement/manage-policy-sets; verified via web search against current HCP Terraform docs, not copied from any official HashiCorp exam material.";

export const SECTIONS = [
  {
    name: "Workspace Workflow Types & Execution Modes",
    subject: "Workspace Types",
    sourceNote: SRC_WORKFLOWS,
    questions: [
      { topic: "VCS-Driven Workflow", stem: "In HCP Terraform's VCS-driven workflow, what triggers a new run in a workspace?", options: ["A webhook fires when new commits are merged/pushed to the linked branch of the workspace's connected VCS repository, automatically queuing a run", "A practitioner must always manually click 'Queue Run' in the UI, even with a VCS repository linked", "The workspace polls the VCS repository for changes once every 24 hours", "A run only starts if a Terraform Cloud administrator manually approves the connection each time"], correct: 0, explanation: "HCP Terraform registers a webhook with the VCS provider when a VCS-driven workspace is created; new commits to the linked branch automatically queue a run, no manual trigger needed." },
      { topic: "VCS-Driven Speculative Plans", stem: "In a VCS-driven workspace, what happens when a pull request is opened against the workspace's linked branch?", options: ["HCP Terraform runs a speculative plan and posts a link to it on the pull request, re-running the plan if the PR is updated — this never applies changes", "HCP Terraform immediately applies the PR's changes to production", "Nothing happens until the PR is merged; pull requests have no effect on runs", "The workspace is automatically deleted and recreated for each PR"], correct: 0, explanation: "Speculative plans preview what a PR's changes would do, posted directly on the PR, without ever proceeding to apply — they update automatically as the PR itself is updated." },
      { topic: "CLI-Driven Workflow", stem: "In the CLI-driven workflow, a practitioner runs 'terraform plan' and 'terraform apply' from their own terminal. Where does the actual Terraform execution happen?", options: ["Remotely in HCP Terraform (assuming remote execution mode) — the CLI streams progress back to the terminal, but the run itself executes on HCP Terraform's infrastructure", "Always on the practitioner's own local machine, identical to not using HCP Terraform at all", "On a dedicated node the practitioner must provision and register themselves, with no HCP Terraform involvement", "In the practitioner's web browser via WebAssembly"], correct: 0, explanation: "The CLI-driven workflow lets you keep using local `terraform` commands, but (in remote execution mode) the actual plan/apply runs remotely in HCP Terraform — your terminal is effectively a live view into that remote run." },
      { topic: "CLI-Driven Setup", stem: "What two things does a practitioner typically need to set up before they can use the CLI-driven workflow against a specific HCP Terraform workspace?", options: ["Authenticate via 'terraform login', and configure a 'cloud' block in the Terraform configuration pointing at the organization/workspace", "Install a special CLI-only binary that replaces the standard 'terraform' executable entirely", "Create an SSH key pair and register its public key with the workspace", "Enable 'terraform workspace new' for every environment, since CLI-driven workspaces require CLI workspaces to be created first"], correct: 0, explanation: "The CLI-driven workflow needs 'terraform login' for authentication and a cloud block in configuration identifying the target HCP Terraform organization and workspace." },
      { topic: "API-Driven Workflow", stem: "What best characterizes the API-driven workflow, compared to VCS-driven and CLI-driven?", options: ["Runs are created directly via calls to the HCP Terraform API — it's the most flexible option but requires building custom tooling/automation around those API calls, unlike the other two which have built-in trigger mechanisms", "It is functionally identical to the VCS-driven workflow, just using a different name", "It's the only workflow type that supports Sentinel policy checks", "It requires disabling remote state storage for the workspace"], correct: 0, explanation: "API-driven is the most flexible workflow since runs are triggered by direct API calls rather than a VCS webhook or local CLI command, but that flexibility means you must build the integration/tooling yourself." },
      { topic: "Remote Execution Mode", stem: "What does a workspace's 'Remote' execution mode mean?", options: ["HCP Terraform performs the actual Terraform runs on its own disposable virtual machines — this is what enables features like policy checks and cost estimation", "Terraform always runs on the practitioner's laptop, with only the final state uploaded afterward", "The workspace has no state storage at all and recomputes everything from scratch on every run", "It refers exclusively to workspaces connected via the API-driven workflow"], correct: 0, explanation: "Remote execution mode means HCP Terraform itself runs the operation on disposable infrastructure it manages, which is a prerequisite for features like Sentinel/OPA policy checks and cost estimation." },
      { topic: "Local Execution Mode Limitation", stem: "A workspace is set to 'Local' execution mode. What is a specific documented limitation of this mode?", options: ["HCP Terraform does not evaluate that workspace's variables or variable sets during local execution — the practitioner's own local values/CLI flags are what actually apply", "Local execution mode disables remote state storage entirely, forcing state to live only on the practitioner's disk", "Local execution mode is only available to organizations on the free tier", "Local execution mode cannot be used with the CLI-driven workflow at all"], correct: 0, explanation: "In local execution mode, HCP Terraform still stores the workspace's state, but it does NOT evaluate that workspace's variables/variable sets — the run instead uses whatever the practitioner supplies locally." },
      { topic: "Agent Execution Mode", stem: "What is the purpose of a workspace's 'Agent' execution mode?", options: ["It lets HCP Terraform run operations via a lightweight, self-hosted HCP Terraform Agent, typically for infrastructure in an isolated or private network HCP Terraform's own cloud runners can't directly reach", "It automatically converts the workspace to use CLI workspaces internally", "It is a synonym for the API-driven workflow", "It disables policy checks to speed up runs in high-throughput pipelines"], correct: 0, explanation: "Agent execution mode routes runs through a self-hosted agent process, which is how HCP Terraform can operate against infrastructure in private networks that its own disposable cloud VMs can't reach directly." },
      { topic: "Auto-Apply: CLI vs. UI/API", stem: "How does triggering an auto-apply differ between the CLI-driven workflow and the UI/API/VCS-driven workflows?", options: ["A CLI-driven apply requires the practitioner to explicitly pass '-auto-approve' to skip confirmation, while UI- and API-triggered runs instead respect whatever auto-apply setting is configured on the workspace itself", "Auto-apply is impossible in the CLI-driven workflow under any circumstances", "The workspace's auto-apply setting applies identically and automatically to every workflow type with no CLI flag needed", "Only VCS-driven workspaces support any form of auto-apply; CLI and API workspaces always require manual confirmation regardless of settings"], correct: 0, explanation: "CLI-driven applies need the practitioner to explicitly opt out of confirmation with -auto-approve, whereas UI/API-triggered runs defer to the workspace-level auto-apply configuration instead of a per-command flag." },
      { topic: "Choosing a Workflow Type", stem: "A team wants runs triggered entirely by their own custom internal deployment platform, with no direct VCS webhook and no one running Terraform CLI commands by hand. Which workflow type fits best?", options: ["API-driven — runs are created via direct calls to the HCP Terraform API from the team's own tooling", "VCS-driven — because any automation should go through a Git webhook regardless of the actual trigger source", "CLI-driven — because it's the most automation-friendly by default", "None of the three workflow types support this use case; it requires Terraform Enterprise specifically"], correct: 0, explanation: "API-driven is designed exactly for this: an external system (here, the team's own deployment platform) drives runs directly through the API rather than relying on a VCS webhook or a human running CLI commands." },
    ],
  },
  {
    name: "Projects: Organizing Workspaces",
    subject: "Projects",
    sourceNote: SRC_PROJECTS,
    questions: [
      { topic: "Workspace-to-Project Relationship", stem: "How many projects can a single HCP Terraform workspace belong to at once?", options: ["Exactly one — every workspace in an organization belongs to exactly one project", "Zero or more; a workspace can exist without being in any project", "Up to three, to allow shared visibility across teams", "As many as the organization has, since project membership is workspace-independent"], correct: 0, explanation: "Every workspace belongs to exactly one project. It's not optional and it isn't a multi-membership relationship." },
      { topic: "Default Project", stem: "What is the 'Default Project' in an HCP Terraform organization?", options: ["A project HCP Terraform automatically creates for every organization, which existing/unassigned workspaces belong to by default — it can be renamed but not deleted", "An optional project a workspace administrator must manually create before any workspaces can exist", "A project that only contains example/demo workspaces provided by HashiCorp", "A project reserved exclusively for the organization's billing and usage settings"], correct: 0, explanation: "Every organization gets an auto-created Default Project; new workspaces land there unless assigned elsewhere, and while it can be renamed, it cannot be deleted." },
      { topic: "No Nested Projects", stem: "Can HCP Terraform projects be nested inside one another (e.g. a 'child' project inside a 'parent' project) to model a team hierarchy?", options: ["No — HCP Terraform's project structure is flat; all projects exist directly under the organization with no parent-child nesting or inheritance between projects", "Yes, projects can be nested up to 5 levels deep", "Yes, but only on Terraform Enterprise, not HCP Terraform's SaaS offering", "Yes, nesting is unlimited and is the primary way permissions are inherited"], correct: 0, explanation: "Projects sit at a single flat level under the organization — there's no nested project hierarchy or automatic inheritance between projects." },
      { topic: "Moving a Workspace Between Projects", stem: "What is a direct consequence of moving an existing workspace from one project to another?", options: ["It can change who has read/write access to that workspace, since team permissions are frequently granted at the project level", "The workspace's Terraform state is permanently wiped and must be re-imported", "All variable values configured on the workspace are automatically reset to their defaults", "The workspace's workflow type (VCS/CLI/API-driven) is forcibly reset to VCS-driven"], correct: 0, explanation: "Because team access is commonly granted at the project level, moving a workspace to a different project can change which teams/users can read or write to it." },
      { topic: "Project-Level Execution Mode Default", stem: "A workspace's execution-mode setting can be set to 'Project Default' rather than explicitly Remote/Local/Agent. What does that mean?", options: ["The workspace inherits whatever default execution mode is configured on the project it belongs to, rather than specifying its own override", "The workspace falls back to Local execution mode automatically, since that's always the project default", "It disables execution entirely until an administrator manually assigns a mode", "'Project Default' only applies to workspaces created via the API-driven workflow"], correct: 0, explanation: "Setting a workspace's execution mode to 'Project Default' means it takes on the execution mode configured at the project level instead of pinning its own explicit value." },
      { topic: "Why Organize by Project", stem: "What is a typical basis teams use for structuring their HCP Terraform projects?", options: ["Grouping workspaces by organizational unit, such as by team, business unit, or service, to align with how resource usage/ownership is actually structured", "Projects must correspond 1:1 with individual AWS/Azure/GCP accounts, with no other grouping allowed", "Projects are auto-generated per Terraform version in use and cannot be organized manually", "Projects must be named after the Terraform module registry namespace they consume"], correct: 0, explanation: "HCP Terraform projects are a flexible grouping mechanism — teams typically structure them around organizational boundaries like team, business unit, or service ownership, not any fixed technical rule." },
      { topic: "State Sharing Scope", stem: "At what levels can cross-workspace state sharing be configured in HCP Terraform?", options: ["At the project level or the organization level", "Only at the individual workspace level, never more broadly", "Only globally across the entire organization, with no per-project option", "State sharing is not a configurable concept in HCP Terraform"], correct: 0, explanation: "HCP Terraform lets you configure state sharing access at the project level (workspaces within/across specific projects) or organization-wide, not just per individual workspace pair." },
      { topic: "Project vs. Tag-Based Grouping", stem: "Besides assigning a workspace to a project, HCP Terraform also lets you attach free-form tags to workspaces. What is one thing tags can be used for that project membership alone does not provide?", options: ["Attaching a policy set (or other org-level configuration) to every workspace matching a given tag, cutting across project boundaries", "Tags are purely cosmetic labels shown in the UI with no functional effect anywhere in HCP Terraform", "Tags replace projects entirely as of the current HCP Terraform version", "Only one tag can ever be applied per workspace, unlike project membership"], correct: 0, explanation: "Tags let you target configuration (like policy sets) at workspaces sharing a tag regardless of which project they're in — a cross-cutting grouping that project membership (a single, exclusive assignment) doesn't provide on its own." },
    ],
  },
  {
    name: "The Run Lifecycle",
    subject: "Run Lifecycle",
    sourceNote: SRC_RUN_LIFECYCLE,
    questions: [
      { topic: "Run Queue Behavior", stem: "Once a run has begun its plan stage in a workspace, what happens to any other run queued for that same workspace?", options: ["It waits — until the current run's plan is confirmed/applied or discarded, HCP Terraform won't start another run in that workspace", "It runs concurrently alongside the first, since HCP Terraform allows unlimited parallel runs per workspace", "It's automatically canceled the moment a newer run is queued", "It's redirected to execute against a different, automatically-created workspace"], correct: 0, explanation: "A workspace processes one run's plan/apply cycle before the next can proceed — a queued run has to wait until the current one reaches a confirmed/applied or discarded state." },
      { topic: "Pending to Plan Transition", stem: "What causes a run sitting in the 'Pending' state to automatically move into the plan stage?", options: ["Being first in that workspace's run queue — HCP Terraform proceeds automatically to plan once nothing else is ahead of it", "A human must manually click 'Start Plan' for every single run, with no automatic progression", "The workspace's linked VCS repository must first pass its CI checks", "Pending runs only advance once a Sentinel policy set has been manually approved"], correct: 0, explanation: "A run automatically proceeds from pending into the plan stage once it's first in the workspace's queue — no manual action is needed to start planning." },
      { topic: "OPA vs. Cost Estimation Ordering", stem: "In the documented run-stage sequence, where does an OPA policy check fall relative to cost estimation?", options: ["OPA policy checks run before cost estimation", "OPA policy checks run after cost estimation but before Sentinel checks", "OPA and cost estimation always run in parallel, never sequentially", "OPA policy checks only run after the apply stage completes"], correct: 0, explanation: "Per the documented run-stage ordering, OPA policy checks occur before cost estimation, with Sentinel policy checks occurring after cost estimation." },
      { topic: "Sentinel vs. Cost Estimation Ordering", stem: "In the documented run-stage sequence, where does a Sentinel policy check fall relative to cost estimation?", options: ["Sentinel policy checks run after cost estimation", "Sentinel policy checks run before cost estimation, ahead of OPA checks", "Sentinel checks replace cost estimation entirely whenever both are enabled", "Sentinel checks only run during the apply stage, never before it"], correct: 0, explanation: "The documented sequence runs cost estimation after any OPA check and before any Sentinel check — so Sentinel checks come after cost estimation, not before it." },
      { topic: "Hard-Mandatory Policy Failure Effect", stem: "A run's plan fails a hard-mandatory (non-overridable) Sentinel policy. What happens to that run?", options: ["The run cannot proceed to apply; a user must fix the underlying configuration issue that caused the failure before a new, passing run can be applied", "The run proceeds to apply anyway, but with the failing resources removed automatically", "Any organization owner can click a single button to override the failure and continue, exactly like a soft-mandatory failure", "The failure is only shown as a warning banner, with apply proceeding normally"], correct: 0, explanation: "A hard-mandatory policy failure blocks the run outright — unlike soft-mandatory (overridable), there is no override path; the configuration itself has to change." },
      { topic: "Run Completion States", stem: "Which of the following is NOT a documented terminal/completion state for an HCP Terraform run?", options: ["Suspended", "Applied", "Discarded", "Errored"], correct: 0, explanation: "Documented completion states include Applied, Planned and Finished, Plan/Apply Errored, Discarded, and Canceled — 'Suspended' is not one of them." },
      { topic: "Discard vs. Confirm", stem: "After a plan completes and is awaiting confirmation, what does discarding it do, as opposed to confirming/applying it?", options: ["Discarding ends that run without applying its changes, freeing the workspace to start its next queued run — the planned changes are simply never carried out", "Discarding permanently deletes the workspace itself", "Discarding automatically re-plans with a completely different configuration version", "Discarding is only possible for VCS-driven runs, never CLI- or API-driven ones"], correct: 0, explanation: "Discarding a completed plan simply abandons it without applying — the run ends, and the workspace becomes free to process its next queued run." },
      { topic: "Speculative Plan Cannot Apply", stem: "Why can a speculative plan (e.g. one triggered by opening a pull request) never be applied directly?", options: ["Speculative plans exist purely to preview likely changes; they are not tied to a confirmable run that can proceed to an apply stage", "Speculative plans are technically identical to normal plans, but HCP Terraform enforces an arbitrary 24-hour delay before allowing apply", "Speculative plans require a separate Enterprise license feature to unlock the apply button", "Speculative plans can be applied, but only by an organization owner, never a regular member"], correct: 0, explanation: "Speculative plans are preview-only by design — they show what would happen without creating an applyable run, which is exactly why they're safe to run automatically against every pull request." },
      { topic: "Run Triggers Between Workspaces", stem: "What is the purpose of a 'run trigger' configured on an HCP Terraform workspace?", options: ["It automatically queues a new run in this workspace whenever a specified SOURCE workspace successfully applies, letting downstream workspaces react to upstream infrastructure changes", "It restricts which VCS branches are allowed to trigger runs in this workspace", "It configures the specific virtual machine size used to execute the workspace's runs", "It sets the maximum number of retries for a failed apply"], correct: 0, explanation: "Run triggers connect workspaces so that a successful apply in one (source) workspace automatically queues a new run in another (dependent) workspace, useful for layered infrastructure." },
      { topic: "Fetching Stage", stem: "What happens during the 'fetching' stage of a VCS-driven run, before planning begins?", options: ["HCP Terraform retrieves the relevant configuration content (and any required modules) from the linked VCS repository at the commit/ref that triggered the run", "HCP Terraform downloads the entire prior state history for auditing purposes", "HCP Terraform fetches billing information to confirm the organization's subscription is current", "Fetching only applies to API-driven runs, never VCS-driven ones"], correct: 0, explanation: "The fetching stage retrieves the actual configuration (source code and referenced modules) from the linked VCS ref before Terraform can generate a plan from it." },
      { topic: "Pre-Plan / Post-Plan Run Tasks", stem: "HCP Terraform supports configurable run tasks (external integrations) that can execute at points like 'pre-plan' or 'post-plan' in the run stage sequence. What does this let an organization do?", options: ["Hook custom external checks or integrations into specific points of the run lifecycle, beyond just the built-in cost-estimation/policy-check stages", "Replace the plan stage entirely with a fully custom, HCP-Terraform-independent process", "Only affect runs triggered by the CLI-driven workflow, never VCS- or API-driven runs", "Run arbitrary Terraform destroy operations automatically before every plan"], correct: 0, explanation: "Run tasks are extension points in the run lifecycle (pre-plan, post-plan, pre-apply, post-apply, etc.) that let external systems participate in or gate a run beyond HCP Terraform's own built-in stages." },
      { topic: "Errored vs. Discarded", stem: "How does a run ending in 'Errored' differ from one ending in 'Discarded'?", options: ["Errored means the plan or apply operation itself failed due to a problem (e.g. a provider error or invalid configuration); Discarded means a user deliberately chose not to proceed with an otherwise-successful plan", "They are two names for exactly the same outcome, used interchangeably in the HCP Terraform UI", "Errored only applies to CLI-driven runs; Discarded only applies to VCS-driven runs", "Discarded means the run failed a policy check; Errored means a user manually canceled it"], correct: 0, explanation: "Errored reflects an actual operational failure during plan/apply; Discarded reflects a deliberate decision to abandon an otherwise-valid, successfully-completed plan without applying it." },
    ],
  },
  {
    name: "Policy Sets: Attaching Sentinel/OPA at Scale",
    subject: "Policy Sets",
    sourceNote: SRC_POLICY,
    questions: [
      { topic: "Policy Set Attachment Levels", stem: "At which levels can an HCP Terraform policy set (Sentinel or OPA) be attached?", options: ["Globally across the whole organization, to specific projects, to specific workspaces, or to workspaces matching a specific tag", "Only at the individual workspace level — organization-wide or project-wide policy attachment isn't supported", "Only at the organization level; per-workspace exceptions aren't possible", "Only to CLI-driven workspaces, since VCS/API-driven runs bypass policy checks entirely"], correct: 0, explanation: "Policy sets are flexible: they can be scoped globally, to specific projects, to specific workspaces, or to any workspace carrying a matching tag." },
      { topic: "Advisory Enforcement Level", stem: "A policy set is attached with enforcement level 'advisory'. What happens if a run's plan fails one of its policies?", options: ["The run is never interrupted — the failure is shown as informational in the UI, and the run proceeds regardless", "The run is blocked exactly like a hard-mandatory failure", "The run is blocked, but any user (regardless of permissions) can override it", "Advisory failures automatically roll back any changes already applied"], correct: 0, explanation: "Advisory is the least strict enforcement level: failed policies are purely informational and never stop the run from proceeding." },
      { topic: "Mandatory Overridable (Soft-Mandatory)", stem: "What distinguishes the 'mandatory overridable' (soft-mandatory) enforcement level from hard-mandatory?", options: ["A soft-mandatory failure stops the run by default, but a user with sufficient permissions can explicitly override it and let the run continue", "Soft-mandatory failures are purely informational, identical to advisory", "Soft-mandatory is only available for OPA policies, never for Sentinel policies", "Soft-mandatory automatically retries the plan with different inputs until it passes"], correct: 0, explanation: "Mandatory overridable blocks the run by default (unlike advisory) but, unlike hard-mandatory, gives an appropriately-permissioned user an explicit path to override the failure and proceed." },
      { topic: "Hard-Mandatory Enforcement", stem: "What happens under the 'mandatory' (hard-mandatory) enforcement level when a policy fails?", options: ["The run is blocked outright with no override option — the underlying issue must actually be fixed before the run (or a new run) can proceed to apply", "It behaves identically to advisory, just with a different label in the UI", "It can still be overridden, but only by an organization owner", "It only applies to Sentinel policies; OPA has no equivalent hard-mandatory level"], correct: 0, explanation: "Hard-mandatory is the strictest level — there's no override mechanism at all; the run simply cannot proceed until the configuration itself changes to satisfy the policy." },
      { topic: "Excluding a Workspace From a Policy Set", stem: "A globally-attached policy set shouldn't apply to one specific workspace that has a legitimate reason to violate it. What can be done?", options: ["That workspace (or project, or tag) can be explicitly excluded from the policy set's scope", "Nothing — a globally-attached policy set cannot have any exceptions carved out", "The entire policy set must be deleted and recreated without global scope", "The workspace must be moved out of the organization entirely to escape a global policy set"], correct: 0, explanation: "Policy sets support explicit exclusions, letting you scope a broadly-attached (e.g. global) policy set away from specific workspaces, projects, or tags that shouldn't be subject to it." },
      { topic: "Tag-Based Policy Attachment", stem: "What is the benefit of attaching a policy set to workspaces matching a specific TAG, rather than attaching it per-project?", options: ["It lets the policy apply across workspaces that share a characteristic regardless of which project each one belongs to, cutting across the project-based grouping", "Tag-based attachment is strictly less flexible and exists only for backward compatibility with older HCP Terraform versions", "Tags can only be used with OPA policies, never Sentinel", "Tag-based attachment removes the need for enforcement levels entirely"], correct: 0, explanation: "Since projects are a flat, exclusive grouping, tag-based policy attachment gives you a second, cross-cutting way to target workspaces sharing something in common (e.g. an environment or compliance need) regardless of project boundaries." },
      { topic: "Policy Check Requires Remote Execution", stem: "Why must a workspace generally use Remote (or Agent) execution mode, rather than Local, for its Sentinel/OPA policy checks to actually run?", options: ["Because policy checks are evaluated as part of HCP Terraform's own managed run pipeline, which local execution mode bypasses by running the operation on the practitioner's own machine instead", "Because Local execution mode is fundamentally incompatible with having any state stored in HCP Terraform at all", "Because Sentinel and OPA are only licensed for use on Terraform Enterprise, not any HCP Terraform execution mode", "There is no such requirement; policy checks run identically regardless of execution mode"], correct: 0, explanation: "Policy enforcement is a stage within HCP Terraform's own managed run pipeline; local execution mode runs the actual Terraform operation on the practitioner's machine, outside that pipeline, so the built-in policy-check stage doesn't apply the same way." },
      { topic: "Policy Sets vs. Sentinel Itself", stem: "What is the relationship between a 'policy set' and an individual Sentinel (or OPA) policy?", options: ["A policy set is a collection that groups one or more individual policies together so they can be deployed and scoped (to workspaces/projects/tags/org) as a unit", "A policy set and a policy are the exact same object, just referred to by two different names in the UI vs. the API", "A policy set can only ever contain exactly one policy", "Policy sets are a Sentinel-only concept; OPA policies cannot be grouped into policy sets at all"], correct: 0, explanation: "A policy set bundles individual policies together as the deployable/attachable unit — you scope and enforce the set, not each policy file individually." },
    ],
  },
];
