// DCA Practice: Container Security Deep Dive
// Fills a confirmed gap: DCA's official 6-domain structure (Orchestration,
// Image Creation/Management/Registry, Installation/Configuration,
// Networking, Security, Storage/Volumes) weights Orchestration and Security
// higher than the other domains per prep-source consensus (re-verified via
// web search 2026-09-04), yet the live DB's only Security-bearing paper
// (dca-security-storage-volumes) folds ~7 security questions in alongside
// ~11 storage/volumes questions — the same or less depth than every other,
// lower-weighted domain gets on its own. This paper goes deep specifically
// on: Docker Content Trust/Notary internals, image vulnerability scanning
// (Docker Scout), secrets management (Swarm secrets vs. bind-mount/env-var
// secrets), seccomp/AppArmor profiles, rootless mode, userns-remap, and TLS
// for the daemon/registry — without repeating the existing paper's ~7
// security questions (docker.sock mount risk, --privileged, DCT purpose,
// seccomp LSM mention, default seccomp behavior, docker scan/Scout mention,
// :latest tag risk — all confirmed via a live DB query before writing this
// file) or the existing Orchestration paper's single Swarm-secrets-vs-env
// question.
//
// IMPORTANT VERIFIED FINDING (web search against docs.docker.com and the
// official Docker blog, 2026-09-04) — DCA's official curriculum still
// treats Docker Content Trust as an examinable topic, but the underlying
// feature is mid-retirement in the real product as of this build date:
//   - Docker Content Trust (DCT) and the notary.docker.io Notary v1 service
//     are being fully retired. Docker ran brownout windows (~4hr outages,
//     writes disabled before reads) in July and August 2026, and the
//     service shuts down completely on December 8, 2026. Ordinary
//     `docker pull`/`docker push` are unaffected — only DCT trust
//     operations (docker trust ..., DOCKER_CONTENT_TRUST=1 signing/
//     verification) are affected.
//   - Docker's own guidance points users to OCI-native alternatives —
//     Sigstore/Cosign and the Notary Project's Notation — which store
//     signatures alongside the image in any compliant registry with no
//     separate trust server to run, unlike DCT/Notary v1's model.
//   - Every DCT question below is written to test the mechanics as they
//     have existed for most of DCT's lifetime (root/repository/snapshot/
//     timestamp key roles, delegation, DOCKER_CONTENT_TRUST=1 behavior) —
//     this remains what DCA's official material still examines — but each
//     includes an explicit retirement note in its explanation/source_note
//     so this paper doesn't misrepresent DCT as a currently-recommended
//     practice going forward.
//   - Docker Scout (docker scout CLI: quickview, cves, compare, policy) is
//     confirmed as Docker's current official image-analysis/vulnerability-
//     scanning product name, superseding the older "docker scan"/Docker
//     Security Scanning branding. Some Scout Dashboard sub-features
//     (health scores, Scout Everywhere, the Sysdig/GitHub SCM/Slack
//     integrations, the Dashboard Policies page, native ECR/ACR
//     integrations) were retired between July 1 and September 1, 2026 —
//     but the core `docker scout` CLI/SBOM/CVE-matching product itself is
//     active and current, so this paper's Scout questions target the CLI
//     surface (cves, quickview, compare, policy), not the retired
//     Dashboard sub-features.
//   - Rootless mode and userns-remap remain distinct, both-current Docker
//     Engine security features as of the 2026-06 (rootless) and 2026-04
//     (userns-remap) doc updates: userns-remap keeps the daemon running as
//     root while remapping container UIDs; rootless mode runs BOTH the
//     daemon and containers without root privileges. The two are not
//     designed to be combined (docs.docker.com/engine/security/rootless
//     confirms userns-remap is unsupported under rootless mode).

export const HEADER_LABEL = "DCA Practice: Container Security Deep Dive";

export const PAPER = {
  slug: "dca-security-deep-dive",
  title: "DCA Practice: Container Security Deep Dive",
  examType: "docker",
  focusTags: ["DCA", "Security", "Docker Content Trust", "Docker Scout", "Rootless", "Secrets"],
  questionCount: 38,
  difficulty: "advanced",
  sortOrder: 5,
  timeLimitMinutes: 60,
  positiveMarks: 1,
  negativeMarks: 0,
};

const SRC_DCT =
  "Security — Docker Content Trust / Notary internals — authored from docs.docker.com's Content Trust documentation and the official Docker blog's retirement/migration guidance; verified via web search 2026-09-04. DCT/Notary v1 is mid-retirement as of this build (brownout windows ran July-August 2026; full shutdown December 8, 2026) — questions test DCT's long-standing mechanics as DCA's curriculum still examines them, but this is flagged here as (needs verification — recheck against current source) for how long DCA continues to examine a retired feature.";
const SRC_SCOUT =
  "Security — Docker Scout image scanning — authored from docs.docker.com/scout documentation (CLI reference: docker scout cves/quickview/compare/policy); verified via web search 2026-09-04 that Docker Scout (not the older docker scan / Docker Security Scanning) is Docker's current official scanning product name, though several Dashboard sub-features (health scores, Scout Everywhere, some integrations) were retired between July-September 2026 — not copied from official Docker exam material.";
const SRC_SECRETS =
  "Security — secrets management — authored from docs.docker.com's Swarm secrets documentation and general container security practice (bind-mounted/env-var secret exposure risks); verified via web search 2026-09-04; not copied from official Docker exam material.";
const SRC_PROFILES =
  "Security — seccomp/AppArmor/kernel isolation — authored from docs.docker.com's seccomp and AppArmor security documentation; verified via web search 2026-09-04; not copied from official Docker exam material.";
const SRC_ROOTLESS =
  "Security — rootless mode & user namespace remapping — authored from docs.docker.com/engine/security/rootless and docs.docker.com/engine/security/userns-remap (rootless doc updated 2026-06, userns-remap doc updated 2026-04, both re-verified via web search 2026-09-04); not copied from official Docker exam material.";
const SRC_TLS =
  "Security — TLS for the Docker daemon and registry — authored from docs.docker.com's 'Protect the Docker daemon socket' documentation and standard registry TLS practice; verified via web search 2026-09-04; not copied from official Docker exam material.";

export const SECTIONS = [
  {
    name: "Docker Content Trust & Notary Internals",
    subject: "Security",
    sourceNote: SRC_DCT,
    questions: [
      { topic: "DCT Key Hierarchy", stem: "Docker Content Trust's underlying Notary model uses a hierarchy of signing keys. Which key, generated on the client and never uploaded to the Notary server, forms the root of trust for a repository?", options: ["The root key", "The snapshot key", "The timestamp key", "The targets/delegation key"], correct: 0, explanation: "The root key is the ultimate trust anchor, generated and kept client-side (offline, ideally); it signs the root metadata that establishes which other keys (targets, snapshot, timestamp) are trusted for a repository. Note: DCT/Notary v1 is mid-retirement (full shutdown December 8, 2026) — this tests the model as DCA's curriculum still examines it." },
      { topic: "DCT Timestamp Key", stem: "In the Notary/DCT trust model, what is the timestamp key's role, and why is it managed server-side rather than by the publisher?", options: ["It signs a short-lived timestamp file confirming the current metadata snapshot is still fresh, protecting against replay/freeze attacks; it's kept server-side because it must be re-signed frequently (near-continuously)", "It records the wall-clock time an image was built, for audit logging only", "It encrypts the image layers during push", "It authenticates the publisher's Docker Hub login session"], correct: 0, explanation: "The timestamp key enables frequent re-signing to prove metadata freshness against freeze/replay attacks; because it must rotate so often, Notary manages it server-side rather than requiring the publisher to sign it manually each time." },
      { topic: "DCT Delegation Keys", stem: "What problem do Notary 'delegation' keys solve in a team publishing workflow under Docker Content Trust?", options: ["They let specific team members or CI systems sign tags for a repository without ever having access to that repository's root or targets key, limiting blast radius if one delegate's key is compromised", "They let any authenticated Docker Hub user sign any repository's images", "They replace the need for a root key entirely", "They are used only to delegate registry storage across multiple regions"], correct: 0, explanation: "Delegations let a repository owner grant scoped signing authority (e.g. to a CI pipeline) without sharing the root/targets key, so a compromised delegate key doesn't compromise the whole repository's trust root." },
      { topic: "DOCKER_CONTENT_TRUST Behavior", stem: "With DOCKER_CONTENT_TRUST=1 set in the client's environment, what happens on `docker pull myregistry/myimage:v2` if that tag was never signed?", options: ["The pull fails with a trust/signature error — DCT enforces that only signed tags can be pulled when enabled", "The pull succeeds, but with a warning printed to stderr", "The pull succeeds, since DOCKER_CONTENT_TRUST only affects docker push, not docker pull", "The pull silently falls back to the ':latest' tag if it is signed"], correct: 0, explanation: "DOCKER_CONTENT_TRUST=1 makes the client enforce signature verification for both push and pull; pulling an unsigned tag fails outright rather than warning-and-continuing." },
      { topic: "docker trust sign", stem: "What does running `docker trust sign myregistry/myimage:v2` actually do, assuming DCT keys already exist for that repository?", options: ["It pushes the image (if not already pushed) and signs its manifest digest, publishing updated trust metadata so DCT-enforcing clients can verify that exact tag", "It only signs the image locally without contacting any registry or trust server", "It generates a brand-new root key for the repository every time it's run", "It re-encrypts the image layers with the signer's private key"], correct: 0, explanation: "docker trust sign associates a cryptographic signature with the specific manifest digest behind a tag and publishes the updated trust data, so verification later confirms that exact content, not just the tag name." },
      { topic: "DCT Scope of Guarantee", stem: "Docker Content Trust guarantees signed, verifiable provenance for a tag's content. What does it NOT guarantee about that image?", options: ["That the image is free of vulnerabilities or malware — DCT proves who published/signed it and that the content is unaltered, not that the content is safe", "That the image manifest digest matches what was signed", "That the signature chains back to a trusted root key", "That the metadata hasn't been tampered with in transit"], correct: 0, explanation: "DCT is a provenance/integrity control, not a vulnerability scanner — a maliciously-built but properly-signed image would still pass DCT verification. Vulnerability scanning is a separate concern (see Docker Scout)." },
      { topic: "DCT Retirement Impact", stem: "Per Docker's own 2026 retirement announcement for Docker Content Trust/Notary v1, which of the following is explicitly true about the transition?", options: ["Ordinary `docker pull`/`docker push` operations are unaffected by the DCT retirement — only DCT-specific trust operations (docker trust ..., DOCKER_CONTENT_TRUST=1 enforcement) stop working once the service is fully shut down", "All Docker Hub image pulls will require re-signing under a new scheme before December 8, 2026", "The retirement only affects self-hosted registries, not Docker Hub", "DCT is being replaced by a drop-in, API-compatible successor also called Notary, requiring no client changes"], correct: 0, explanation: "Docker's retirement guidance is explicit that only DCT/Notary-specific trust operations are affected; the described migration path is to OCI-native tools like Sigstore/Cosign or Notation, not a compatible in-place replacement." },
    ],
  },
  {
    name: "Vulnerability Scanning with Docker Scout",
    subject: "Security",
    sourceNote: SRC_SCOUT,
    questions: [
      { topic: "docker scout cves", stem: "What does `docker scout cves myimage:latest` do?", options: ["Performs a local vulnerability analysis of the specified image (or the most recently built image if none is given), matching its SBOM against a vulnerability database and reporting CVEs", "Uploads the image to Docker Hub for public scanning only", "Lists the CVEs of every image ever pulled on the host, regardless of argument", "Deletes any image found to have a critical CVE"], correct: 0, explanation: "docker scout cves runs Scout's CVE analysis against the named image (defaulting to the most recently built one), based on the SBOM Scout compiles for it." },
      { topic: "docker scout quickview", stem: "How does `docker scout quickview` differ from `docker scout cves` in its default output?", options: ["quickview gives a condensed summary (vulnerability counts plus base-image refresh/update recommendations) rather than the full per-CVE detail that cves reports", "quickview only works against images already pushed to Docker Hub, cves works on any local image", "quickview scans for license issues only, never vulnerabilities", "There is no functional difference; quickview is a deprecated alias for cves"], correct: 0, explanation: "quickview is the summary view — vulnerability counts and base-image update suggestions — while cves gives the full, detailed CVE listing." },
      { topic: "docker scout compare", stem: "What is `docker scout compare` used for?", options: ["Analyzing and comparing two images (e.g. a new build vs. its previous release, or vs. a different base) to show what vulnerabilities/packages changed between them", "Comparing two Dockerfiles line-by-line for syntax differences", "Comparing CPU/memory resource usage between two running containers", "Comparing two Swarm services' replica counts"], correct: 0, explanation: "compare analyzes two images against each other, surfacing the vulnerability/package delta — useful for confirming a rebuild actually reduced exposure rather than just changing the tag." },
      { topic: "SBOM Basis", stem: "What does Docker Scout compile for an image as the basis for its vulnerability matching, before checking any CVE database?", options: ["A Software Bill of Materials (SBOM) — an inventory of the packages/components actually present in the image", "A full decompilation of every binary in the image", "A checksum of the entire image tarball only", "A list of every Dockerfile instruction used to build the image"], correct: 0, explanation: "Scout builds an SBOM describing the image's actual package/component inventory, then matches that inventory against a continuously updated vulnerability database to identify CVEs." },
      { topic: "EPSS Filtering", stem: "What do the `--epss-score` and `--epss-percentile` flags on `docker scout cves` let you do?", options: ["Filter/prioritize reported CVEs by their Exploit Prediction Scoring System likelihood of real-world exploitation, rather than by CVSS severity alone", "Set a hard limit on how many CVEs are displayed, regardless of severity", "Filter results to only CVEs affecting the base image, excluding application layers", "Convert the output into SARIF format automatically"], correct: 0, explanation: "EPSS scores estimate real-world exploitation likelihood; filtering by EPSS score/percentile helps prioritize remediation toward CVEs more likely to actually be exploited, a different axis than raw CVSS severity." },
      { topic: "Scout Output Formats", stem: "Why would a team export `docker scout cves` results in SARIF format specifically?", options: ["SARIF (Static Analysis Results Interchange Format) is a standard many CI/CD platforms and code-scanning dashboards (e.g. GitHub code scanning) can natively ingest and display", "SARIF is required for Scout to run at all", "SARIF format compresses the output to reduce storage costs", "SARIF is the only format Scout supports for local images"], correct: 0, explanation: "SARIF is a widely-supported interchange format for static analysis tooling, letting Scout's findings flow directly into CI/CD and code-scanning UIs that already consume that format." },
      { topic: "Scout vs. DCT Scope", stem: "A team has both Docker Content Trust and Docker Scout enabled. If an image is properly signed under DCT but Scout reports several critical CVEs in it, what does this indicate?", options: ["The two tools check different things — DCT confirmed the image's authenticity/integrity (who published it, unaltered), while Scout independently found real vulnerabilities in its actual package contents; a valid signature says nothing about vulnerability-free content", "This is a contradiction that means one of the tools is misconfigured", "Scout automatically revokes DCT signatures when it finds critical CVEs", "DCT signing implies Scout scanning was already run and passed"], correct: 0, explanation: "Signing (DCT) and vulnerability scanning (Scout) are orthogonal controls — a signed image can still contain real vulnerabilities, since DCT proves provenance/integrity, not security posture." },
      { topic: "Docker Scout Naming History", stem: "Docker Scout is the current name for Docker's official image-analysis and vulnerability-scanning product. What was this capability's earlier, now-superseded branding?", options: ["docker scan / Docker Security Scanning", "Docker Trusted Registry Insights", "Docker Bench for Security", "Notary Vulnerability Service"], correct: 0, explanation: "Docker's scanning capability was previously branded as docker scan / Docker Security Scanning before being superseded by Docker Scout, which is the current officially-documented product as of this build's web-search verification (2026-09-04)." },
    ],
  },
  {
    name: "Secrets Management",
    subject: "Security",
    sourceNote: SRC_SECRETS,
    questions: [
      { topic: "Secret Delivery Mechanism", stem: "When a Swarm secret is attached to a service, how is it made available inside a task's container, and why is this considered more secure than an environment variable?", options: ["It's mounted as a file under /run/secrets/<secret_name> in an in-memory (tmpfs) filesystem — unlike env vars, it isn't captured by `docker inspect`, isn't inherited by child processes automatically, and never touches disk", "It's injected as a regular environment variable, identical to -e, but with an encrypted name", "It's written to a persistent named volume shared across all replicas", "It's passed as a command-line argument to the container's entrypoint"], correct: 0, explanation: "Swarm secrets land as files in an in-memory /run/secrets/ mount inside the container — this avoids the common env-var pitfalls (visible in `docker inspect`, leaked into crash dumps/logs, inherited by subprocesses, sometimes visible via /proc)." },
      { topic: "Why Not Env Vars", stem: "Beyond visibility in `docker inspect`, what is a concrete risk of passing a database password via `-e DB_PASSWORD=...` on a standalone `docker run` versus a Swarm secret?", options: ["Environment variables are commonly captured in crash dumps, logged by process managers or `/proc/<pid>/environ`, and are often inherited by every child process spawned inside the container — a much larger accidental-exposure surface than a single, access-controlled file", "Environment variables are always transmitted in plaintext over the network, while secrets are always encrypted in transit", "Docker automatically encrypts environment variables at rest but not files", "There is no real difference — this is a common misconception"], correct: 0, explanation: "The practical risk is exposure surface: env vars proliferate into child processes, /proc, and various logging/monitoring tooling by default, whereas a secret file's exposure is limited to processes that explicitly read that path." },
      { topic: "Secrets Are Swarm-Scoped", stem: "Can a standalone container (started with plain `docker run`, outside of Swarm mode) consume a secret created with `docker secret create`?", options: ["No — Docker secrets are a Swarm-services feature; a standalone container has no mechanism to mount a Swarm secret (bind-mounting a file or using --mount type=bind on a manually-managed file is the closest standalone equivalent, with none of the same access-scoping guarantees)", "Yes, via the --secret flag on docker run, identical to service create", "Yes, but only if the container is run with --privileged", "Yes, automatically, if the container's image was built with a SECRET instruction in its Dockerfile"], correct: 0, explanation: "docker secret create/--secret only apply to Swarm services (docker service create/docker stack deploy); there is no standalone `docker run --secret` mechanism." },
      { topic: "Secret Immutability & Rotation", stem: "A Swarm secret's content cannot be updated in place once created. What is the standard pattern for rotating a secret's value (e.g. a rotated database password) for a running service?", options: ["Create a new secret under a new name/version (e.g. db_password_v2), then update the service to reference the new secret and remove the reference to the old one — Swarm reschedules tasks to pick up the new mount", "Run `docker secret update <name>` with the new value, which Swarm pushes to all running tasks live", "Delete the old secret first; Swarm automatically regenerates it with prompted new input", "Restart the Docker daemon, which reloads all secrets from disk with new values"], correct: 0, explanation: "Because secret content is immutable once created, rotation means creating a new secret object and updating the service definition to point at it — there is no in-place `docker secret update` for content." },
      { topic: "Secret File Ownership", stem: "By default, what UID/GID and file mode does a mounted secret file have inside the container, and can this be changed?", options: ["It defaults to root ownership (UID 0) with mode 0444 (read-only); this can be overridden per-secret at service-create time with flags like --secret-uid, --secret-gid, and --secret-mode", "It always inherits the UID of the container's ENTRYPOINT process automatically, with no way to override it", "It is always world-writable (0666) for compatibility with non-root application processes", "Secret files have no concept of ownership or permissions; Docker enforces access purely at the network layer"], correct: 0, explanation: "Secrets default to root:root ownership with 0444 permissions, but docker service create exposes --secret-uid/--secret-gid/--secret-mode to customize this for applications that need to read the file as a non-root user." },
      { topic: "Configs vs. Secrets", stem: "Swarm also has a `docker config create` resource, distinct from secrets. What is the key functional difference between a Swarm 'config' and a Swarm 'secret'?", options: ["Configs are for non-sensitive data (e.g. an nginx.conf template) and are NOT encrypted at rest in the same access-restricted way secrets are, while secrets are specifically for sensitive material with tighter access controls", "Configs and secrets are functionally identical; 'config' is simply an older, deprecated name for 'secret'", "Configs can only be used with Kubernetes-backed MKE clusters, never plain Swarm", "Configs are mutable in place, while nothing else about them differs from secrets"], correct: 0, explanation: "Configs and secrets share a similar mounting mechanism, but configs are designed for non-sensitive configuration data (and, unlike secrets, can be inspected via `docker config inspect` without redaction) — secrets are the mechanism intended for actual sensitive material." },
      { topic: "Secrets in Compose for Stacks", stem: "In a Compose file deployed via `docker stack deploy`, how is a secret typically supplied — via a top-level `secrets:` key referencing a `file:` — actually delivered to Swarm, given Swarm secrets are meant to avoid plaintext-on-disk exposure?", options: ["`docker stack deploy` reads the referenced local file once at deploy time and creates a genuine Swarm secret object from its content — the plaintext file on the deploying host is a one-time input, not something Swarm continues to reference", "The file path itself is bind-mounted read-only into every replica directly from the deploying host's disk", "Compose secrets are purely cosmetic in stack deploys and are silently ignored; only docker secret create populates real secrets", "The file is embedded directly into the image at the next `docker build`"], correct: 0, explanation: "For a file-based Compose secret, `docker stack deploy` reads the local file at deploy time and creates a Swarm secret object from its contents; from then on, Swarm's own secret distribution (not the original file) is what tasks receive." },
    ],
  },
  {
    name: "Kernel-Level Isolation: seccomp & AppArmor",
    subject: "Security",
    sourceNote: SRC_PROFILES,
    questions: [
      { topic: "Custom seccomp Profiles", stem: "How does supplying a custom seccomp profile via `--security-opt seccomp=/path/to/profile.json` differ from Docker's default seccomp profile?", options: ["The default profile already blocks a broad set of dangerous syscalls (around 44 by default) as a sane baseline for all containers; a custom profile lets you further restrict (or, less commonly, loosen) exactly which syscalls a specific container's process is permitted to make, tailored to what that application actually needs", "The default profile allows all syscalls; only a custom profile enables any blocking at all", "seccomp only takes effect if AppArmor is disabled first", "Custom seccomp profiles can only add permitted syscalls, never remove any that the default already allows"], correct: 0, explanation: "Docker ships a default seccomp profile blocking a substantial syscall set already; a custom profile lets you tune this further per-workload, following least-privilege (though a badly written custom profile could also loosen restrictions)." },
      { topic: "Disabling seccomp", stem: "What does `--security-opt seccomp=unconfined` do, and what is the security implication?", options: ["It disables seccomp filtering entirely for that container, allowing it to make any syscall the kernel/other isolation layers (capabilities, AppArmor, etc.) don't otherwise block — this significantly increases kernel-attack surface and should only be used when a specific, understood syscall need requires it", "It applies the strictest possible seccomp profile, blocking every syscall except a minimal allowlist", "It only disables seccomp for the container's init process (PID 1), not its child processes", "It is the default setting for all containers unless overridden"], correct: 0, explanation: "unconfined removes the seccomp filter entirely, expanding the syscall surface exposed to the container — generally discouraged except for specific, well-understood cases (e.g. certain debugging/tracing tools)." },
      { topic: "AppArmor vs. seccomp", stem: "AppArmor and seccomp both provide kernel-level restriction, but at different granularities. What is the key conceptual difference between them?", options: ["seccomp restricts which syscalls a process may invoke at all (syscall-number granularity); AppArmor is a Mandatory Access Control (MAC) system that additionally restricts what a process can do with specific resources (e.g. which files/paths it may read/write/execute, network access), using named profiles per-binary or per-container", "They are functionally redundant; enabling one automatically disables the other", "AppArmor only works on Fedora/RHEL-family kernels while seccomp only works on Debian/Ubuntu-family kernels", "seccomp controls network access exclusively; AppArmor controls filesystem access exclusively, with no overlap"], correct: 0, explanation: "seccomp is a syscall filter; AppArmor is a path/resource-based MAC system layered on top, restricting what a confined process can do with files, capabilities, and network access beyond the syscall level." },
      { topic: "Docker's Default AppArmor Profile", stem: "On a Docker host with AppArmor enabled (e.g. Ubuntu), what happens to a container by default if no custom AppArmor profile is specified?", options: ["Docker applies its own default profile (docker-default), which restricts a range of actions (like mounting most filesystems and loading kernel modules) without requiring any manual configuration", "No AppArmor confinement is applied unless the operator explicitly specifies one with --security-opt apparmor=...", "AppArmor is applied only to Swarm services, never to standalone containers", "The container inherits the AppArmor profile of the process that invoked `docker run`, not a Docker-specific one"], correct: 0, explanation: "Docker automatically generates and applies a docker-default AppArmor profile to containers on AppArmor-enabled hosts unless a different profile (or `unconfined`) is explicitly requested." },
      { topic: "Combining seccomp and AppArmor", stem: "Is it valid and typical to run a container with BOTH a custom seccomp profile AND a custom AppArmor profile applied simultaneously?", options: ["Yes — they operate at different layers (syscall-level vs. resource/MAC-level) and are commonly combined for defense-in-depth, alongside Linux capabilities dropping", "No — Docker only allows one security module to be active per container, and AppArmor takes precedence if both are specified", "No — specifying both causes the container to fail to start with a conflict error", "Yes, but only on hosts running SELinux, since SELinux arbitrates between the two"], correct: 0, explanation: "seccomp and AppArmor are complementary, independent layers; combining a tailored seccomp profile with a tailored AppArmor profile (and dropped capabilities) is standard container-hardening practice, not a conflict." },
    ],
  },
  {
    name: "Rootless Docker & User Namespace Remapping",
    subject: "Security",
    sourceNote: SRC_ROOTLESS,
    questions: [
      { topic: "Rootless vs. userns-remap", stem: "What is the key difference between running Docker in 'rootless mode' and enabling 'userns-remap' on a conventionally-installed (root) daemon?", options: ["In rootless mode, BOTH the Docker daemon and the containers it runs operate entirely without root privileges on the host; with userns-remap, the daemon itself still runs as root, and only the container's UID namespace is remapped so its 'root' maps to an unprivileged host UID", "They are interchangeable names for the same feature", "userns-remap runs everything rootless, while rootless mode still requires a root-owned daemon process", "Rootless mode only affects networking; userns-remap only affects filesystem permissions"], correct: 0, explanation: "docs.docker.com's own comparison states this explicitly: userns-remap keeps a root daemon but remaps container UIDs, while rootless mode removes root privileges from the daemon itself as well as the containers." },
      { topic: "Rootless + userns-remap Compatibility", stem: "Can userns-remap be enabled together with rootless mode on the same Docker installation?", options: ["No — userns-remap is not a supported configuration under rootless mode per current Docker documentation; attempting to combine them is not a supported setup", "Yes, and this combination is Docker's officially recommended hardened configuration", "Yes, but only when using the overlay2 storage driver", "This combination is required for Swarm mode to function correctly"], correct: 0, explanation: "Current Docker documentation and the moby/moby issue tracker confirm userns-remap is not supported under rootless mode — the two features address overlapping concerns via different, non-composable mechanisms." },
      { topic: "Rootless Mode Limitations", stem: "Running Docker in rootless mode comes with some documented functional trade-offs compared to a conventional root daemon. Which of these is a real, documented rootless-mode limitation?", options: ["Certain features that require elevated host privileges — such as some network drivers, cgroup-based resource limiting on older kernels/cgroup configurations, and binding to privileged (< 1024) ports without extra setup — are restricted or require workarounds", "Rootless mode cannot run any container images at all, only Dockerfiles built with a ROOTLESS instruction", "Rootless mode disables image layer caching entirely", "Rootless mode cannot be used with Docker Compose under any circumstances"], correct: 0, explanation: "Rootless mode's documented limitations include restrictions around certain network drivers, cgroup-based resource controls depending on the host's cgroup setup, and binding privileged ports without additional configuration (e.g. slirp4netns/rootlesskit port forwarding, CAP_NET_BIND_SERVICE workarounds)." },
      { topic: "userns-remap Mechanism", stem: "Mechanically, what does enabling userns-remap actually change about how a container's root user (UID 0 inside the container) is represented on the host?", options: ["UID 0 inside the container is mapped to a non-zero, unprivileged UID range on the host, so even a process that gains 'root' inside the container has no genuine root privileges on the underlying host", "It disables the concept of UID 0 entirely, so no process inside any container can ever be UID 0", "It only remaps GIDs, leaving UID mapping untouched", "It changes the host's own root UID to a different value"], correct: 0, explanation: "userns-remap maps the container's UID/GID range (including its apparent 'root', UID 0) onto an unprivileged range of host UIDs/GIDs, so container-root escaping to the host doesn't grant real host-root." },
      { topic: "Why This Matters for Container Breakout", stem: "From a threat-model perspective, what specific class of attack does userns-remap primarily mitigate that Docker's default (non-remapped) configuration does not?", options: ["A container-breakout vulnerability that would otherwise grant the attacker genuine host-root, instead only grants the attacker the unprivileged remapped host UID — substantially limiting post-breakout impact", "SQL injection attacks against an application running inside the container", "Man-in-the-middle attacks against the container's outbound network traffic", "Supply-chain attacks where a malicious base image is pulled from a public registry"], correct: 0, explanation: "userns-remap specifically narrows the blast radius of a kernel/runtime-level container-escape: what would otherwise be a host-root compromise becomes a compromise of an unprivileged host account instead." },
    ],
  },
  {
    name: "TLS for the Docker Daemon & Registry",
    subject: "Security",
    sourceNote: SRC_TLS,
    questions: [
      { topic: "Unauthenticated Daemon Socket Risk", stem: "By default, if the Docker daemon is configured to listen on a TCP socket (e.g. `-H tcp://0.0.0.0:2375`) with no additional configuration, what is the security implication?", options: ["Anyone who can reach that port has unauthenticated root-equivalent control of the host, since the Docker API allows actions like mounting the host filesystem into a container — this port should never be exposed without TLS client-certificate authentication", "The connection is automatically encrypted with TLS by default, but not authenticated", "Only local (127.0.0.1) clients can connect, regardless of the bind address", "Docker requires a username/password over this port even without extra configuration"], correct: 0, explanation: "Port 2375 (plain TCP, no TLS) is explicitly documented as insecure — anyone reaching it gets unauthenticated daemon control, which is effectively root on the host given what the Docker API can do (e.g. bind-mounting `/`)." },
      { topic: "Daemon TLS Port Convention", stem: "What TCP port is the conventional default for a TLS-secured Docker daemon socket, distinct from the insecure plain-TCP default?", options: ["2376", "2375", "8080", "443"], correct: 0, explanation: "2375 is the conventional plain (insecure) daemon port; 2376 is the conventional TLS-secured daemon port — using 2376 alone doesn't secure anything without also actually configuring TLS certs (--tlsverify and related flags)." },
      { topic: "Daemon TLS Client Verification", stem: "Configuring the daemon with `--tlsverify` (plus --tlscacert, --tlscert, --tlskey) does what specifically, beyond just encrypting the connection?", options: ["It requires connecting clients to present a certificate signed by a trusted CA, so only clients holding a valid client certificate can authenticate to the daemon at all — not just encrypting traffic, but authenticating who's allowed to connect", "It only encrypts traffic; any client can still connect without a certificate", "It restricts connections to the local host only, regardless of the bind address", "It disables the REST API entirely in favor of a gRPC-only interface"], correct: 0, explanation: "--tlsverify enables mutual TLS: the daemon requires a client certificate signed by the configured CA, so encryption alone isn't the point — unauthorized clients are rejected outright." },
      { topic: "Registry TLS Requirement", stem: "By default, will the Docker client refuse to push/pull from a private registry served over plain HTTP (no TLS)?", options: ["Yes — Docker treats any registry without valid TLS as insecure by default and refuses to communicate with it unless that registry is explicitly added to the daemon's 'insecure-registries' allowlist", "No — Docker communicates with any registry over HTTP by default with no special configuration required", "Only Docker Hub enforces TLS; self-hosted registries are always allowed over plain HTTP", "TLS is only enforced for docker push, never for docker pull"], correct: 0, explanation: "Docker refuses non-TLS registries by default; an operator must explicitly list a registry under insecure-registries in the daemon config to permit plain-HTTP communication with it — an explicit, auditable opt-out." },
      { topic: "Self-Signed Registry Certs", stem: "A private registry uses a certificate signed by an internal/self-signed CA rather than a public CA. What must be done on each Docker host that needs to pull from it, without marking the registry as 'insecure'?", options: ["The internal CA's certificate must be placed under /etc/docker/certs.d/<registry-host:port>/ca.crt on each host so the daemon trusts it for that specific registry, without disabling TLS verification globally", "Nothing — Docker automatically trusts any certificate presented by a registry on first connection (trust-on-first-use)", "The registry must be added to /etc/hosts with a special docker-trusted alias", "The daemon must be restarted with --tlsverify=false globally"], correct: 0, explanation: "Docker supports per-registry CA trust via certs.d/<host:port>/ca.crt, letting an internal CA be trusted for a specific registry without weakening TLS verification for every other registry (which marking it 'insecure' would effectively do)." },
      { topic: "mTLS for Registry Access", stem: "Beyond a registry presenting a valid server certificate, some registries also require client certificates for pull/push (mutual TLS). Where does the Docker client look for a matching client cert/key pair for a given registry host?", options: ["Under /etc/docker/certs.d/<registry-host:port>/, alongside any CA cert, using files with .cert and .key extensions", "In the user's ~/.docker/config.json credential store exclusively, with no filesystem cert lookup", "Client certificates aren't supported for registry authentication at all — only username/password or token auth", "In the same location as the daemon's own TLS certs (/etc/docker/tls/), regardless of which registry is being accessed"], correct: 0, explanation: "The same per-registry certs.d/<host:port>/ directory holds both the CA cert (for verifying the registry) and, if required, a client .cert/.key pair (for the registry to verify the client) — registry TLS config is host-scoped under that path." },
    ],
  },
];
