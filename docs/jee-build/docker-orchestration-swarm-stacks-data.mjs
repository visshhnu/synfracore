// DCA Practice: Orchestration Deep Dive — Swarm Services & Stacks
// Fills the same confirmed domain-weighting gap as the companion Security
// Deep Dive paper: DCA's official 6-domain structure weights Orchestration
// higher than most other domains per prep-source consensus (re-verified via
// web search 2026-09-04), yet the live DB has only ONE Orchestration paper
// (dca-orchestration-swarm-kubernetes, 22Q) — the same or less depth than
// several lower-weighted domains get. This paper goes deep specifically on:
// Swarm service update strategies/rollback, placement constraints and
// preferences, secrets/configs scoped to Swarm services in stack deploys,
// multi-service stack deployment mechanics, Swarm overlay networking
// specifics, and Mirantis Kubernetes Engine (MKE)-specific RBAC/teams/
// organizations concepts — without repeating the existing paper's 22
// questions (confirmed via a live DB query before writing this file:
// swarm init, manager vs. worker, odd-manager-count quorum rationale,
// service concept, replicated vs. global mode, scale command, overlay
// network's general purpose, routing mesh purpose, update parallelism vs.
// delay, default pause-on-failed-health-check behavior, the MKE product
// name itself, stack definition, docker-compose vs. stack deploy, node
// labels + placement constraints as a single combined question, node ls,
// drain availability, Raft consensus, quorum-loss effect, Swarm secrets
// vs. env vars purpose, task-vs-service relationship, service logs, and a
// general Kubernetes-via-MKE-over-Swarm rationale question) or the
// companion Security Deep Dive paper's secrets-management section (that
// paper covers secrets vs. env vars generally; this paper covers
// configs/secrets specifically as Swarm SERVICE/STACK resources — target
// paths, external references, and stack-deploy delivery mechanics).
//
// Facts below verified via web search against docs.docker.com (2026-09-04),
// not from memory alone:
//  - --update-failure-action accepts pause (default), continue, or
//    rollback; rollback triggers automatically when the failed-task
//    fraction exceeds --update-max-failure-ratio.
//  - --update-parallelism / --rollback-parallelism can differ (e.g. update
//    one task at a time, roll back three at a time).
//  - Encrypted overlay networks use `--opt encrypted` on `docker network
//    create --driver overlay`, applying IPSEC encryption at the VXLAN
//    level; this is opt-in (not default) due to a real performance cost,
//    and is not supported for Windows containers.
//  - The `ingress` overlay network is auto-created on `docker swarm init`/
//    `docker swarm join` and handles the routing mesh's control/data
//    traffic for published service ports by default.
//  - MKE's access-control model is grant-based: a grant is a 1:1:1 mapping
//    of subject (user, organization, team, or service account) + role +
//    resource set — Swarm resources are grouped into "collections", while
//    Kubernetes resources are grouped into namespaces.

export const HEADER_LABEL = "DCA Practice: Orchestration Deep Dive — Swarm Services & Stacks";

export const PAPER = {
  slug: "dca-orchestration-swarm-stacks-deep-dive",
  title: "DCA Practice: Orchestration Deep Dive — Swarm Services & Stacks",
  examType: "docker",
  focusTags: ["DCA", "Orchestration", "Swarm", "Stacks", "MKE", "RBAC"],
  questionCount: 39,
  difficulty: "advanced",
  sortOrder: 6,
  timeLimitMinutes: 60,
  positiveMarks: 1,
  negativeMarks: 0,
};

const SRC_UPDATES =
  "Orchestration — Swarm service update strategies & rollback — authored from docs.docker.com's `docker service update`/`docker service create` CLI reference and the Swarm rolling-update tutorial; verified via web search 2026-09-04; not copied from official Docker exam material.";
const SRC_PLACEMENT =
  "Orchestration — Swarm placement constraints & preferences — authored from docs.docker.com's Swarm service placement documentation; verified via web search 2026-09-04; not copied from official Docker exam material.";
const SRC_CONFIGS =
  "Orchestration — secrets/configs as Swarm service & stack resources — authored from docs.docker.com's Swarm configs/secrets documentation and Compose file reference (top-level configs:/secrets: keys); verified via web search 2026-09-04; not copied from official Docker exam material.";
const SRC_STACKS =
  "Orchestration — multi-service stack deployment — authored from docs.docker.com's `docker stack deploy` CLI reference and Compose-file-for-stacks documentation; verified via web search 2026-09-04; not copied from official Docker exam material.";
const SRC_OVERLAY =
  "Orchestration — Swarm overlay networking specifics — authored from docs.docker.com's overlay network driver and 'Manage swarm service networks' documentation; verified via web search 2026-09-04; not copied from official Docker exam material.";
const SRC_MKE_RBAC =
  "Orchestration — Mirantis Kubernetes Engine (MKE) RBAC, teams & organizations — authored from docs.mirantis.com's MKE role-based access control and grants documentation; verified via web search 2026-09-04; not copied from official Docker/Mirantis exam material.";

export const SECTIONS = [
  {
    name: "Swarm Service Update Strategies & Rollback",
    subject: "Orchestration",
    sourceNote: SRC_UPDATES,
    questions: [
      { topic: "update-failure-action Values", stem: "The `--update-failure-action` flag on `docker service create`/`update` accepts which set of values?", options: ["pause (default), continue, and rollback", "stop, restart, and retry", "abort, skip, and force", "wait, escalate, and terminate"], correct: 0, explanation: "docs.docker.com confirms exactly three values: pause (the default — halts the rollout for manual intervention), continue (proceeds despite failures, risking a mixed-version cluster), and rollback (automatically reverts)." },
      { topic: "Automatic Rollback Trigger", stem: "With `--update-failure-action=rollback` configured, what specifically triggers the automatic rollback during a rolling update?", options: ["The fraction of tasks that failed to update successfully exceeds the threshold set by `--update-max-failure-ratio`", "Any single task failure, regardless of the configured failure ratio", "The update simply running longer than `--update-monitor`'s duration", "A manual `docker service rollback` command must always be issued first, even with this flag set"], correct: 0, explanation: "Automatic rollback under this setting is threshold-based: it fires once the observed failure ratio during the update exceeds --update-max-failure-ratio, not on the very first failure." },
      { topic: "update-order: start-first vs. stop-first", stem: "What does setting `--update-order start-first` change about how Swarm performs a rolling update, compared to the default `stop-first`?", options: ["Swarm starts the new task and waits for it to become healthy BEFORE stopping the corresponding old task, briefly running both versions side-by-side — useful for avoiding downtime, at the cost of temporarily needing extra resource capacity", "It updates all tasks simultaneously instead of in batches", "It reverses the update to always process the newest-created task first", "It disables health checks during the update entirely"], correct: 0, explanation: "start-first prioritizes availability by briefly overlapping old and new tasks (new one must be healthy before the old one stops), whereas the default stop-first tears down the old task before starting the replacement." },
      { topic: "Differing Update/Rollback Parallelism", stem: "A service is configured with `--update-parallelism 1 --rollback-parallelism 3`. What does this achieve?", options: ["Normal rolling updates proceed cautiously, one task at a time, but if a rollback is triggered, it happens faster by reverting three tasks at a time", "Updates happen 3 tasks at a time, but rollbacks are forced to happen 1 task at a time for safety", "The rollback-parallelism value is ignored unless update-parallelism is also set to 3", "This configuration is invalid; the two values must always match"], correct: 0, explanation: "Update and rollback parallelism are independently configurable — a common pattern is a cautious, slow rollout with a fast rollback path, since reverting to known-good code carries less risk than rolling forward." },
      { topic: "Manual Rollback Command", stem: "Beyond an automatic rollback triggered by --update-failure-action=rollback, how can an operator manually revert a service to its previous spec after noticing a bad rollout?", options: ["`docker service update --rollback <service>` (or the shorthand `docker service rollback <service>`), which reverts the service to its previously recorded specification", "Manually re-running the exact previous `docker service create` command, since Swarm keeps no history of prior specs", "Deleting and recreating the service from a backed-up Compose file only — there is no live rollback mechanism", "`docker service pause <service>` followed by a manual re-image of every task"], correct: 0, explanation: "Swarm retains the previous service spec specifically to support `docker service update --rollback`/`docker service rollback`, letting an operator revert without needing external state." },
      { topic: "update-monitor Duration", stem: "What does the `--update-monitor` duration (e.g. `--update-monitor 30s`) control during a rolling update?", options: ["How long Swarm waits after updating each task before deciding whether that task's update actually succeeded or failed, before moving on to the next batch", "The total maximum time allowed for the entire rolling update across all tasks combined", "How often the CLI polls and prints progress output to the terminal", "The TTL for the service's DNS entry during the update window"], correct: 0, explanation: "update-monitor is a per-task observation window — Swarm watches each updated task for this duration to catch failures (e.g. a crash loop) before considering that task's update complete and proceeding." },
      { topic: "update-delay Between Batches", stem: "Distinct from update-monitor, what does `--update-delay` control?", options: ["The pause Swarm inserts between updating one batch of tasks (per --update-parallelism) and starting the next batch", "The delay before the very first task in the update begins, only", "The delay before Swarm even accepts the `docker service update` command", "The DNS propagation delay for the service's virtual IP"], correct: 0, explanation: "update-delay is the inter-batch pacing control — after one parallelism-sized batch finishes (and its update-monitor window passes), Swarm waits this long before starting the next batch." },
    ],
  },
  {
    name: "Placement Constraints & Preferences",
    subject: "Orchestration",
    sourceNote: SRC_PLACEMENT,
    questions: [
      { topic: "Built-in Node Constraints", stem: "Which of the following is a built-in Swarm placement constraint expression usable without any custom labeling, e.g. to restrict a service to manager nodes only?", options: ["--constraint 'node.role==manager'", "--constraint 'node.type==master'", "--constraint 'swarm.manager==true'", "--constraint 'node.primary==yes'"], correct: 0, explanation: "node.role is a built-in constraint key (values: manager or worker) that requires no custom label setup, unlike custom node.labels.* constraints." },
      { topic: "Custom Node Label Constraints", stem: "After running `docker node update --label-add zone=us-east node-3`, how would a service be constrained to place tasks only on nodes carrying that label?", options: ["--constraint 'node.labels.zone==us-east'", "--constraint 'zone==us-east'", "--label 'zone==us-east'", "--constraint 'engine.labels.zone==us-east'"], correct: 0, explanation: "Custom labels set via docker node update are referenced in constraints under the node.labels.<key> namespace, distinguishing them from Docker Engine daemon labels (engine.labels.*)." },
      { topic: "engine.labels vs. node.labels", stem: "What is the distinction between a constraint referencing `engine.labels.<key>` versus one referencing `node.labels.<key>`?", options: ["engine.labels reads labels set on the Docker Engine daemon itself (via daemon.json or dockerd flags on that host), while node.labels reads labels Swarm-management assigned to the node object via `docker node update --label-add` — they're set through entirely different mechanisms", "They are synonyms with no functional difference", "engine.labels only applies to worker nodes; node.labels only applies to managers", "engine.labels is deprecated and always empty in current Docker versions"], correct: 0, explanation: "engine.labels come from the daemon's own configuration (host-level, set independent of Swarm), while node.labels are Swarm-level metadata an administrator assigns via docker node update — both are valid constraint namespaces but sourced differently." },
      { topic: "Multiple Constraints — AND Logic", stem: "A service is created with both `--constraint 'node.labels.zone==us-east'` and `--constraint 'node.labels.tier==frontend'`. How are multiple --constraint flags combined?", options: ["With AND logic — a node must satisfy ALL specified constraints simultaneously to be eligible for task placement", "With OR logic — a node satisfying any one of the constraints is eligible", "Only the last --constraint flag specified takes effect; earlier ones are overridden", "Multiple --constraint flags on the same service are rejected as invalid syntax"], correct: 0, explanation: "Multiple constraints are ANDed together — every one must hold for a given node to be considered eligible, narrowing the eligible node set with each additional constraint." },
      { topic: "Constraint Inequality Operator", stem: "What does the constraint `--constraint 'node.role!=worker'` express?", options: ["Only place tasks on nodes where the role is NOT worker (i.e., effectively manager nodes) — != is a valid constraint operator alongside ==", "This is invalid syntax; only == is supported for constraints", "It excludes the constraint from being evaluated at all", "It places tasks preferentially, but not exclusively, away from worker nodes"], correct: 0, explanation: "Swarm constraint expressions support both == and != operators, so this is a valid (if slightly unusual, compared to just using node.role==manager) way to express the same restriction." },
      { topic: "Placement Preferences (Soft Spread)", stem: "How does a placement PREFERENCE (`--placement-pref 'spread=node.labels.zone'`) differ in effect from a placement CONSTRAINT?", options: ["A preference is a soft/best-effort hint that influences how tasks are spread across nodes (e.g. evenly across zones) without excluding any node outright, whereas a constraint is a hard requirement that excludes non-matching nodes entirely", "Preferences and constraints are functionally identical; --placement-pref is just older syntax for --constraint", "A preference guarantees perfectly equal distribution across all matching values, with no exceptions", "Preferences can only be used for global mode services, never replicated services"], correct: 0, explanation: "Placement preferences (spread) are a soft-scheduling hint for even distribution across a label's values (e.g. balancing replicas across zones/racks); unlike constraints, they never make a node ineligible — they just influence which eligible node is chosen." },
      { topic: "max-replicas-per-node", stem: "What does `--replicas-max-per-node` (used with `docker service create/update`) control?", options: ["The maximum number of this service's tasks that may be scheduled onto any single node, preventing over-concentration of replicas on one host", "The total maximum replica count for the entire service across the whole cluster", "The maximum number of different services allowed to run on one node", "The maximum number of manager nodes allowed to host any task of this service"], correct: 0, explanation: "replicas-max-per-node caps per-node concentration of a single service's tasks, which is a different concern from the service's overall --replicas count (total desired tasks cluster-wide)." },
    ],
  },
  {
    name: "Secrets & Configs as Swarm Service/Stack Resources",
    subject: "Orchestration",
    sourceNote: SRC_CONFIGS,
    questions: [
      { topic: "docker config create Basics", stem: "What does `docker config create app_config ./nginx.conf` do?", options: ["Creates a Swarm 'config' object named app_config from the local file's contents, which can then be attached to one or more services", "Creates a new Swarm secret, since configs and secrets share the same underlying command with different flags", "Immediately mounts the file into every currently-running service on the cluster", "Validates the file as a syntactically correct nginx configuration before accepting it"], correct: 0, explanation: "docker config create registers a named config object from file content in the Swarm's Raft store, available for later attachment to services — it doesn't automatically attach or validate anything." },
      { topic: "Attaching a Config to a Service", stem: "When creating a service with `--config source=app_config,target=/etc/nginx/nginx.conf`, what determines the file path the config is mounted at inside the task's container?", options: ["The `target` parameter — it explicitly sets the in-container mount path, which can differ from the config's own name", "The config is always mounted at /run/configs/<config-name>, with no way to override the path", "The target parameter only works for secrets, not configs — configs always mount at their source name under /", "The container's WORKDIR determines the mount path automatically"], correct: 0, explanation: "The target parameter explicitly controls the in-container path, decoupling the config's registered name from where it actually lands inside the container's filesystem." },
      { topic: "Configs Are Node-Portable", stem: "A Swarm service using a config is rescheduled from one worker node to another (e.g. after the original node fails). What happens to the config's availability on the new node?", options: ["The config is automatically delivered to whichever node the rescheduled task lands on — configs (like secrets) are Swarm-cluster-scoped resources, not tied to a specific node's local filesystem", "The task fails to start on the new node unless the config file was manually copied there first", "The config is lost permanently and must be recreated", "The task starts, but with an empty file at the target path until manually re-synced"], correct: 0, explanation: "Configs and secrets are both distributed via Swarm's Raft-backed cluster state, not node-local files — rescheduling to any manager-approved node still delivers the config correctly." },
      { topic: "Compose external: true for Configs/Secrets", stem: "In a stack's Compose file, a top-level secret is declared as `secrets: { db_password: { external: true } }`. What does `external: true` mean in this context?", options: ["The secret is expected to already exist in the Swarm (created beforehand via `docker secret create`), and `docker stack deploy` should reference it rather than try to create it from a file/inline value", "The secret's value is fetched from an external HTTPS endpoint at every task start", "The secret is shared externally with other Swarm clusters automatically", "external: true disables encryption for that specific secret"], correct: 0, explanation: "external: true tells stack deploy the named secret/config is pre-existing and managed outside this Compose file's lifecycle — deploy will fail if it doesn't already exist, rather than attempting to create it." },
      { topic: "Config/Secret Name Aliasing in Compose", stem: "A Compose file for a stack declares a top-level secret `db_password` with `file: ./db_password.txt`, and a service references it under `secrets: [db_password]`. If the stack is deployed with `docker stack deploy -c compose.yml mystack`, what is the actual name of the created Swarm secret object?", options: ["mystack_db_password — stack deploy prefixes top-level secret/config names with the stack name to avoid collisions between stacks reusing the same short name", "db_password, exactly as written, with no prefixing", "A randomly generated UUID, unrelated to the Compose-file name", "It depends on the Compose file's `version:` key; only version 3.9+ applies the stack-name prefix"], correct: 0, explanation: "Stack deploy namespaces top-level secrets/configs (and most other resources) under `<stackname>_<resourcename>`, so multiple stacks can each define a same-named `db_password` without colliding in the cluster's global secret/config namespace." },
      { topic: "Updating a Service's Attached Configs", stem: "A running service currently uses `app_config`. To roll out a changed configuration file, what is the correct sequence, given config content (like secret content) is immutable once created?", options: ["Create a new config object under a new name (e.g. app_config_v2) from the updated file, then `docker service update --config-rm app_config --config-add source=app_config_v2,target=<path> <service>` to swap it in, triggering a rolling redeploy of the service's tasks", "Overwrite the original file on disk; Swarm automatically detects the file change and re-syncs it to all tasks live", "Run `docker config update app_config ./new-nginx.conf` to update its content in place", "Restart the Docker daemon on each node to force a config re-read"], correct: 0, explanation: "Like secrets, config content is immutable — updating it means creating a new config object and using --config-rm/--config-add on the service, which reschedules tasks to pick up the new attachment." },
    ],
  },
  {
    name: "Multi-Service Stack Deployment",
    subject: "Orchestration",
    sourceNote: SRC_STACKS,
    questions: [
      { topic: "Minimum Compose Version for Stacks", stem: "What is the minimum Compose file format version required for a Compose file to be usable with `docker stack deploy`?", options: ["Version 3 (e.g. version: '3' or higher) — stacks require the version-3.x schema, which added Swarm-specific keys like `deploy:`", "Version 1, the original Compose format", "Version 2, since it introduced named networks", "Stacks work with any version, including version 1, since stack deploy silently ignores unsupported keys"], correct: 0, explanation: "The Compose v3 schema specifically introduced the deploy: key (replicas, placement, update_config, resources, etc.) needed to express Swarm-service concepts — v1/v2 files aren't structured to carry that information." },
      { topic: "build: Key Ignored by Stack Deploy", stem: "A stack's Compose file includes a service with a `build:` key instead of (or alongside) `image:`. What happens when this is deployed with `docker stack deploy`?", options: ["The build: key is ignored — `docker stack deploy` does not build images; the service must already reference a pre-built image available to every node (typically via a registry), and stack deploy will fail or behave unexpectedly if no valid image: is resolvable", "Swarm automatically builds the image on the manager node and distributes it to all workers", "Each worker node independently builds the image locally before starting its own tasks", "docker stack deploy delegates the build to Docker Hub automatically"], correct: 0, explanation: "Unlike `docker compose up` (which can build locally), `docker stack deploy` has no build step — images must already be built and available (usually via a shared registry) to every node that might run a task." },
      { topic: "Default Stack Networking", stem: "If a stack's Compose file doesn't declare a custom top-level `networks:` section, what networking do the stack's services get by default?", options: ["Stack deploy creates a default overlay network scoped to that stack, and attaches every service in the stack to it automatically, letting services reach each other by service name", "All services fall back to using the host network directly", "Services get no network at all and cannot communicate unless a network is explicitly declared", "Services are attached to the default `bridge` network, identical to standalone docker run containers"], correct: 0, explanation: "Stack deploy provisions a default stack-scoped overlay network when none is declared, so services can resolve and reach each other by name — this is analogous to Compose's default network for `docker compose up`, but using the overlay driver instead of bridge." },
      { topic: "docker stack ps", stem: "What does `docker stack ps mystack` show, as distinct from `docker stack services mystack`?", options: ["The individual tasks (with their current/desired state, node placement, and any recent error) across every service in the stack — a more granular view than the per-service summary `stack services` gives", "The exact same output as `docker stack services`, just with different column ordering", "Only tasks that have failed, filtering out running ones", "The historical list of every docker stack deploy invocation ever run against that stack name"], correct: 0, explanation: "stack ps operates at task granularity (like docker service ps, but across the whole stack), while stack services summarizes at the service level (replica counts, image, ports) — different levels of detail for different debugging needs." },
      { topic: "Redeploying an Updated Stack", stem: "After editing a stack's Compose file (e.g. bumping an image tag), what is the correct way to apply the change to the running stack?", options: ["Re-run `docker stack deploy -c compose.yml mystack` with the same stack name — stack deploy is declarative and reconciles the running stack toward the new file's desired state, updating only what changed", "Run `docker stack rm mystack` first, then deploy fresh — in-place stack updates are not supported", "Manually run `docker service update` against each affected service individually; stack deploy cannot be re-run on an existing stack", "Restart the Swarm manager nodes to force a re-read of the Compose file"], correct: 0, explanation: "docker stack deploy is idempotent/declarative — re-running it against the same stack name reconciles differences (new image, changed replica count, etc.) without requiring a teardown first." },
      { topic: "Stack Resource Naming/Prefixing", stem: "A stack named `mystack` defines a service called `web` in its Compose file. What is the actual Swarm service name created?", options: ["mystack_web — stack deploy prefixes service (and most other resource) names with the stack name to avoid collisions across stacks", "web, exactly as declared, with no prefixing applied to service names specifically", "A randomly generated name unrelated to either the stack or service name", "mystack-web, using a hyphen rather than an underscore separator"], correct: 0, explanation: "Consistent with how stack deploy namespaces secrets/configs/networks, services are also prefixed as <stackname>_<servicename>, letting the same short service name be reused safely across different stacks." },
      { topic: "docker stack rm Scope", stem: "What does `docker stack rm mystack` remove?", options: ["Every Swarm resource stack deploy created and attributed to that stack — its services, its stack-scoped networks, and (in current Docker versions) its stack-scoped secrets/configs — but NOT any external:true secrets/configs it only referenced", "Only the stack's services; networks, secrets, and configs must be removed manually afterward in every case", "The entire Swarm cluster's configuration, resetting it to an unmanaged state", "Only stops the stack's tasks without deleting the service definitions, so `docker stack deploy` on the same name resumes instantly with no reconciliation"], correct: 0, explanation: "stack rm tears down the resources that stack deploy itself created and owns for that stack; resources marked external: true were never owned by the stack, so they're left alone." },
    ],
  },
  {
    name: "Swarm Overlay Networking Specifics",
    subject: "Orchestration",
    sourceNote: SRC_OVERLAY,
    questions: [
      { topic: "Creating an Encrypted Overlay Network", stem: "How is application-data encryption enabled for traffic on a specific overlay network between Swarm nodes?", options: ["`docker network create --opt encrypted --driver overlay <name>`, which applies IPSEC encryption at the VXLAN level for that network's data traffic", "Overlay networks are always encrypted by default; no flag is needed", "By setting `--encrypt-daemon` when starting dockerd, which encrypts every network cluster-wide", "By attaching a `tls=true` label to each service using the network"], correct: 0, explanation: "The --opt encrypted flag on network create (overlay driver) turns on IPSEC-based encryption of the VXLAN data plane for that specific network — application data among Swarm nodes is NOT encrypted by default." },
      { topic: "Encrypted Overlay Trade-off", stem: "What is the documented trade-off of enabling `--opt encrypted` on an overlay network?", options: ["A non-negligible performance penalty from the IPSEC encryption/decryption overhead, so it's recommended to test the impact before using it in production, and it isn't supported for Windows containers on that network", "It doubles the number of manager nodes required for quorum", "It disables the routing mesh for any service attached to that network", "It requires switching the entire cluster from VXLAN to a different tunneling protocol"], correct: 0, explanation: "docs.docker.com explicitly documents the performance cost of overlay encryption and calls out that it isn't supported for Windows containers on an encrypted overlay network." },
      { topic: "The ingress Network", stem: "What automatically creates the special `ingress` overlay network, and what is its role?", options: ["`docker swarm init` (and joining nodes via `docker swarm join`) automatically creates it; it handles control/data traffic for the routing mesh, and any service published without attaching a user-defined overlay network uses it by default", "An administrator must manually run `docker network create ingress` after initializing the swarm", "It's created only when the first service is published with -p, and is deleted after that service is removed", "It exists only on manager nodes, never on workers"], correct: 0, explanation: "ingress is provisioned automatically as part of swarm initialization/joining, and by default backs the routing mesh for services published without a custom overlay network." },
      { topic: "Overlay Network Scope: attachable", stem: "What does the `--attachable` flag on `docker network create --driver overlay` enable that isn't possible on a non-attachable overlay network?", options: ["Standalone containers (not just Swarm service tasks) can be explicitly attached to that overlay network via `docker run --network`, letting non-service containers communicate with Swarm services on it", "It allows the network to span multiple separate Swarm clusters", "It converts the overlay network into a bridge network automatically", "It's required before any service can use the network at all — non-attachable overlay networks reject all connections"], correct: 0, explanation: "By default, Swarm overlay networks are only usable by Swarm service tasks; --attachable specifically opens the network up so standalone containers can also join it via docker run --network." },
      { topic: "Overlay Network Underlying Ports", stem: "For the overlay driver's VXLAN data plane to function correctly between nodes, which port/protocol must be open in addition to the Swarm management ports (2377/tcp, 7946/tcp+udp)?", options: ["UDP port 4789 (VXLAN)", "TCP port 443 (HTTPS)", "TCP port 2376 (Docker daemon TLS)", "UDP port 53 (DNS)"], correct: 0, explanation: "VXLAN, the encapsulation protocol overlay networking uses, communicates over UDP/4789 — this must be open between nodes alongside the Swarm control-plane ports (2377/tcp for manager join, 7946/tcp+udp for the gossip/discovery protocol)." },
      { topic: "Overlay vs. Bridge Driver Scope", stem: "What is the fundamental scope difference between Docker's `overlay` network driver and its `bridge` network driver?", options: ["overlay networks span multiple Docker hosts, enabling containers/tasks on different nodes to communicate directly as if on the same network; bridge networks are confined to a single host", "bridge networks span multiple hosts; overlay networks are single-host only", "They are functionally identical, differing only in name", "overlay is used exclusively for Kubernetes workloads under MKE, never for Swarm"], correct: 0, explanation: "This is the core distinction motivating overlay's existence: bridge networking works only within one Docker host, while overlay uses VXLAN encapsulation to let containers on different hosts communicate as though co-located." },
    ],
  },
  {
    name: "Mirantis Kubernetes Engine (MKE) — RBAC, Teams & Organizations",
    subject: "Orchestration",
    sourceNote: SRC_MKE_RBAC,
    questions: [
      { topic: "The Grant Model", stem: "In MKE's access-control model, what is a 'grant', structurally?", options: ["A 1:1:1 mapping of subject (a user, organization, team, or service account) + role + resource set — defining who has how much access to what", "A single global on/off switch for cluster-wide admin access", "A time-limited access token issued for a single API call", "A synonym for a Kubernetes ClusterRoleBinding, with no Swarm equivalent"], correct: 0, explanation: "MKE's own documentation describes a grant as exactly this triple: a specific subject, given a specific role, over a specific resource set — the basic unit of MKE's RBAC system." },
      { topic: "Resource Sets: Collections vs. Namespaces", stem: "How does MKE group cluster resources for the purpose of RBAC grants, and does this differ between its Swarm and Kubernetes orchestration modes?", options: ["Swarm resources are grouped into 'collections'; Kubernetes resources are grouped into namespaces — the two orchestrators use different resource-grouping concepts within the same overall grant model", "Both Swarm and Kubernetes resources use identical 'namespace' grouping, with no separate Swarm-specific concept", "Resources cannot be grouped at all in MKE; every grant must name individual resources one at a time", "Collections and namespaces are purely cosmetic labels with no actual effect on grant scope"], correct: 0, explanation: "MKE documentation explicitly distinguishes Swarm 'collections' from Kubernetes namespaces as the two orchestrator-specific ways cluster resources are grouped for grant targeting." },
      { topic: "Organizations and Teams", stem: "In MKE, what is the relationship between an 'organization' and a 'team' when selecting a grant's subject?", options: ["A team is a subgroup within an organization; when granting access to an organization-based subject, an administrator can optionally narrow the grant to one specific team within that organization rather than the whole organization", "Teams and organizations are interchangeable terms for the exact same object", "A team can span multiple organizations, but an organization cannot contain multiple teams", "Teams only exist for Kubernetes-orchestrated resources, never for Swarm collections"], correct: 0, explanation: "MKE's grant-creation workflow lets an admin pick an Organization and then optionally a Team within it, letting access be scoped more finely than 'the whole organization' when needed." },
      { topic: "Service Accounts as Grant Subjects", stem: "Besides individual users, organizations, and teams, what other subject type can an MKE grant be issued to?", options: ["Service accounts — allowing automated/non-human clients (e.g. a CI pipeline) to be granted scoped access the same way a human user or team would be", "Only Kubernetes Pods directly, never an account abstraction", "Only the built-in 'admin' role, which cannot be reassigned", "Physical worker nodes themselves, as if they were users"], correct: 0, explanation: "MKE's grant subject options include Users, Organizations (with optional Team), and Service Accounts — covering both human and automated/non-human access patterns under the same grant model." },
      { topic: "Default vs. Custom Roles", stem: "What is the difference between an MKE 'default role' and a 'custom role' in the context of building a grant?", options: ["Default roles are pre-built permission sets MKE ships with out of the box (e.g. read-only, restricted-control, full-control); custom roles let an administrator define their own permitted API operations per resource type when the defaults don't match a needed access level", "Default roles apply only to Kubernetes; custom roles apply only to Swarm", "Custom roles can only reduce a default role's permissions, never add new ones", "There is no functional difference; 'custom role' is just a display-name alias for a chosen default role"], correct: 0, explanation: "MKE ships default roles as common starting points, but also supports custom roles built by explicitly selecting which API operations are permitted per resource type — for access patterns the defaults don't cover." },
      { topic: "Cluster Role Binding Switch for Kubernetes", stem: "When creating a Kubernetes-oriented grant in MKE, what does enabling the 'Cluster Role Binding' switch do, as opposed to leaving it off?", options: ["It applies the chosen role binding across ALL namespaces in the cluster, rather than scoping the grant to a single selected namespace", "It converts the grant into a Swarm-collection-based grant instead of a namespace-based one", "It immediately deletes any existing namespace-scoped grants for that same subject", "It has no functional effect; it's purely a UI label"], correct: 0, explanation: "The Cluster Role Binding switch is how an MKE administrator chooses between a namespace-scoped grant and a cluster-wide one when working with Kubernetes-orchestrated resources." },
    ],
  },
];
