// Terraform Associate Practice: Validation, Lifecycle & Ephemeral Values
// Fills a confirmed gap vs. the current HashiCorp Terraform Associate (004)
// exam (tests Terraform 1.12; 003 was retired 2026-01-08): a live DB query
// against the 4 existing "terraform" exam_type papers (2026-09-04) found
// custom variable validation touched only once at a definitional level
// (terraform-associate-iac-fundamentals-workflow Q28/Q29 — what a
// validation/precondition block IS, not multi-condition or cross-variable
// usage), lifecycle meta-arguments covered only at single-question
// definitional depth for 3 of 5 (terraform-associate-configuration-deep-dive
// Q2-4: prevent_destroy, create_before_destroy, ignore_changes — no
// replace_triggered_by, no precondition/postcondition depth), and ZERO
// questions anywhere on Ephemeral Values / Write-Only Arguments, a genuinely
// new mechanism (Terraform 1.10/1.11) that 004 tests as its own topic.
//
// Facts below verified via web search against developer.hashicorp.com and
// hashicorp.com (2026-09-04) — not from training-data memory, since this
// feature (ephemeral values, write-only arguments, 1.9's cross-variable
// validation) postdates most pretrained knowledge of Terraform:
//  - Ephemeral values/blocks: developer.hashicorp.com/terraform/language/
//    manage-sensitive-data/ephemeral and .../language/block/ephemeral —
//    ephemeral resources use `ephemeral "<TYPE>" "<LABEL>" {}`, are never
//    stored in state or plan files, are referenced as ephemeral.<TYPE>.<LABEL>,
//    and follow an open -> (optional periodic renew) -> close lifecycle tied
//    to the operations that actually need them.
//  - Write-only arguments: developer.hashicorp.com/terraform/plugin/framework/
//    resources/write-only-arguments — introduced in Terraform 1.11, provider-
//    defined arguments (conventionally suffixed `_wo`) whose prior/planned/
//    final state values are always null (so they can't drive plan diffs on
//    their own), can accept ephemeral or non-ephemeral values, cannot be used
//    with set attributes/set nested attributes/set nested blocks, and are
//    conventionally paired with a companion "version" argument (e.g.
//    password_wo_version) to signal the provider when to re-apply the value.
//  - ephemeralasnull(): developer.hashicorp.com/terraform/language/functions/
//    ephemeralasnull — available Terraform v1.10+, takes an ephemeral value
//    (including nested inside an object) and returns null, letting an
//    otherwise-ephemeral-tainted value be used where a null is acceptable.
//  - Custom conditions (precondition/postcondition): developer.hashicorp.com/
//    terraform/language/expressions/custom-conditions — both are declared
//    INSIDE a `lifecycle {}` block on a resource, data source, or output
//    (not standalone); precondition runs after planning but before the
//    resource/data source/output is created/read; postcondition runs after
//    apply (or after a data source read). Both take condition + error_message.
//  - Cross-variable validation: hashicorp.com/blog/terraform-1-9-enhances-
//    input-variable-validations — as of Terraform 1.9, a variable's
//    validation { condition } can reference other input variables, data
//    sources, and locals (previously it could only reference the variable
//    itself) — but the condition must STILL include a reference to the
//    variable being validated; referencing only other objects is invalid.
// Current Terraform Associate 004 exam guide (per developer.hashicorp.com/
// terraform/tutorials/certification-004/associate-review-004 as of this
// build) targets Terraform 1.12 — no version-specific behavior below is
// newer than that.

export const HEADER_LABEL = "Terraform Associate Practice: Validation, Lifecycle & Ephemeral Values";

export const PAPER = {
  slug: "terraform-validation-lifecycle-ephemeral",
  title: "Terraform Associate Practice: Validation, Lifecycle & Ephemeral Values",
  examType: "terraform",
  focusTags: ["Terraform Associate", "Validation", "Lifecycle", "Ephemeral Values"],
  questionCount: 38,
  difficulty: "advanced",
  sortOrder: 5,
  timeLimitMinutes: 60,
  positiveMarks: 1,
  negativeMarks: 0,
};

const SRC_VALIDATION =
  "Custom Validation Rules — authored from developer.hashicorp.com/terraform/language/values/variables, .../language/block/variable, and hashicorp.com/blog/terraform-1-9-enhances-input-variable-validations; verified via web search against current Terraform 1.9+/1.12 docs, not copied from any official HashiCorp exam material.";
const SRC_LIFECYCLE =
  "Lifecycle Meta-Arguments & Custom Conditions — authored from developer.hashicorp.com/terraform/language/meta-arguments/lifecycle and .../language/expressions/custom-conditions; verified via web search against current Terraform docs, not copied from any official HashiCorp exam material.";
const SRC_EPHEMERAL =
  "Ephemeral Values & Write-Only Arguments — authored from developer.hashicorp.com/terraform/language/manage-sensitive-data/ephemeral, .../language/block/ephemeral, .../language/functions/ephemeralasnull, and .../plugin/framework/resources/write-only-arguments; verified via web search against current Terraform 1.10-1.12 docs (this feature postdates most pretrained knowledge), not copied from any official HashiCorp exam material.";

export const SECTIONS = [
  {
    name: "Custom Validation Rules",
    subject: "Validation",
    sourceNote: SRC_VALIDATION,
    questions: [
      { topic: "Validation Block Required Arguments", stem: "What are the two required arguments inside a variable's 'validation' block?", options: ["condition and error_message", "condition and severity", "rule and message", "check and fail_message"], correct: 0, explanation: "A validation block requires exactly two arguments: condition (a boolean expression) and error_message (the string shown when condition evaluates to false)." },
      { topic: "Multiple Validation Blocks", stem: "Can a single input variable declare more than one 'validation' block?", options: ["No — a variable may have at most one validation block; multiple constraints must be combined into a single condition", "Yes — a variable can declare multiple validation blocks, each checked independently with its own error_message", "Yes, but only if the variable's type is a primitive type like string or number", "No — additional constraints must be enforced with a separate lifecycle precondition instead"], correct: 1, explanation: "Terraform allows multiple validation blocks on the same variable; each is evaluated independently and reports its own error_message if its condition fails, which is often clearer than one large compound condition." },
      { topic: "Validation Timing", stem: "At what point does Terraform evaluate a variable's validation block(s)?", options: ["During terraform plan, after the value is known but before Terraform proceeds to build the rest of the plan", "Only during terraform apply, never during terraform plan", "Only when terraform validate is run explicitly, never during plan/apply", "After every resource in the configuration has already been created"], correct: 0, explanation: "Variable validations run early during plan (once the variable's value is known), before Terraform evaluates the rest of the configuration that depends on it." },
      { topic: "Cross-Variable Validation (1.9+)", stem: "As of Terraform 1.9, what changed about what a variable's validation condition is allowed to reference?", options: ["It can now also reference other input variables, data sources, and local values in the same module, not just the variable being declared", "It can now reference resources in a completely different Terraform workspace's remote state", "It can now call out to an external HTTP endpoint to validate the value", "Nothing changed; validation conditions have always been able to reference any object in the configuration"], correct: 0, explanation: "Terraform 1.9 lifted the earlier restriction that a validation condition could only reference the variable itself, allowing conditions to also consider other variables, data sources, and locals — enabling genuine cross-variable validation." },
      { topic: "Cross-Variable Validation Constraint", stem: "A variable's validation block now (Terraform 1.9+) references only var.enable_feature and var.other_setting, but never references the variable it's actually attached to. What happens?", options: ["Terraform still allows it and evaluates the cross-references correctly", "Terraform reports an invalid validation block: the condition must still include a reference to the variable being validated, even when referencing other objects", "Terraform silently ignores the condition and always treats it as true", "Terraform automatically infers the missing self-reference and adds it"], correct: 1, explanation: "Being able to reference other variables/data sources/locals doesn't remove the requirement that the condition also reference the variable it's declared on — a validation block that never mentions its own variable is rejected." },
      { topic: "Validation vs. Type Constraints", stem: "A variable is declared with type = number. Why might you still add a validation block on top of that type constraint, e.g. requiring the number be between 1 and 100?", options: ["Because type constraints only check the value's basic type (that it IS a number), not business-logic bounds on what a valid number actually is — validation adds that semantic check", "Because type constraints are deprecated in favor of validation blocks", "Because validation blocks are required for every numeric variable by the Terraform language", "Because type = number alone would otherwise accept string input silently"], correct: 0, explanation: "Type constraints only enforce the value's shape/type; a validation block is needed to enforce domain-specific rules like numeric ranges, allowed strings, or cross-field consistency." },
      { topic: "Multi-Condition Validation", stem: "You need a variable's validation to fail unless BOTH of two independent conditions hold. Besides writing two separate validation blocks, what's a common way to express this in a single condition?", options: ["Combine both boolean sub-expressions with && (or the alltrue() function over a list of conditions) inside one validation block's condition", "Use two separate error_message arguments in the same validation block", "Nest a second validation block inside the first one's condition", "It cannot be done in a single condition; two validation blocks are mandatory"], correct: 0, explanation: "A single condition can combine multiple boolean checks with && (logical AND) or alltrue([...]) so the whole validation block fails unless every sub-condition is satisfied." },
      { topic: "Validation on Complex Types", stem: "Can a validation block's condition inspect elements inside a variable declared with a complex type, e.g. list(object({ name = string, port = number }))?", options: ["Yes — the condition can use expressions like a 'for' expression or alltrue([for x in var.foo : x.port > 0]) to check every element", "No — validation blocks only work on primitive-typed variables (string, number, bool)", "Yes, but only the first element of the list can be checked", "No — complex types must be validated exclusively via a separate lifecycle precondition on a resource"], correct: 0, explanation: "Validation conditions are ordinary Terraform expressions, so they can use for-expressions and functions like alltrue() to check every element of a complex-typed variable, not just primitives." },
      { topic: "error_message Content", stem: "What is the error_message argument in a validation block used for?", options: ["The human-readable message displayed to the user when the condition evaluates to false, explaining what constraint was violated", "An internal error code that Terraform logs but never displays", "A regular expression used to further validate the value", "A fallback default value applied when the condition fails"], correct: 0, explanation: "error_message is purely a user-facing string explaining why the value failed validation — it plays no role in the pass/fail logic itself, which is entirely determined by condition." },
      { topic: "Validation Failure Behavior", stem: "When a variable's validation condition evaluates to false, what happens to the Terraform run?", options: ["Terraform halts with an error showing the validation's error_message, and does not proceed to build a plan", "Terraform logs a warning but proceeds with the plan using the invalid value", "Terraform silently substitutes the variable's default value instead", "Terraform prompts interactively for a corrected value before continuing"], correct: 0, explanation: "A failed validation is a hard stop — Terraform reports the error_message and aborts the plan; it does not continue with an invalid value or fall back silently." },
      { topic: "Validation vs. sensitive/ephemeral", stem: "Can a variable be marked both 'sensitive = true' (or 'ephemeral = true') AND still have a validation block?", options: ["Yes — sensitive/ephemeral marking and validation are independent features and can be combined on the same variable", "No — sensitive and ephemeral variables are exempt from all validation", "Only sensitive variables can have validation; ephemeral variables cannot", "Only ephemeral variables can have validation; sensitive variables cannot"], correct: 0, explanation: "sensitive and ephemeral control how a value is displayed/persisted, which is orthogonal to validation, which checks the value's content — both can be applied to the same variable." },
    ],
  },
  {
    name: "Lifecycle Meta-Arguments & Custom Conditions",
    subject: "Lifecycle",
    sourceNote: SRC_LIFECYCLE,
    questions: [
      { topic: "create_before_destroy", stem: "A resource change requires replacement, and the resource has 'lifecycle { create_before_destroy = true }'. What order does Terraform use?", options: ["Terraform creates the new replacement resource first, and only destroys the old one after the new one is successfully created", "Terraform always destroys the old resource first, then creates the new one, regardless of this setting", "Terraform creates and destroys both resources simultaneously in parallel", "This setting has no effect on ordering; it only affects logging verbosity"], correct: 0, explanation: "create_before_destroy reverses Terraform's default destroy-then-create replacement order, creating the new resource before removing the old one — useful for minimizing downtime." },
      { topic: "prevent_destroy", stem: "A resource has 'lifecycle { prevent_destroy = true }'. What happens if a plan would destroy that resource (e.g. its resource block is removed from configuration)?", options: ["Terraform refuses to proceed with the plan/apply and returns an error, protecting the resource from destruction", "Terraform destroys the resource anyway but logs a warning first", "Terraform automatically comments out the destroy operation and skips only that resource, applying everything else", "prevent_destroy only blocks 'terraform destroy' at the root, not resource removal from configuration"], correct: 0, explanation: "prevent_destroy acts as a safety guard: any plan that would destroy the resource (including one caused by removing it from configuration) is rejected with an error instead of proceeding." },
      { topic: "ignore_changes", stem: "A resource has 'lifecycle { ignore_changes = [tags] }'. What effect does this have?", options: ["Terraform stops treating out-of-band or subsequent changes to the tags attribute as drift requiring correction in future plans, leaving whatever value is actually present", "Terraform prevents the tags attribute from ever being set on initial creation", "Terraform ignores changes to every attribute on the resource, not just tags", "Terraform automatically reverts tags to its original value on every apply"], correct: 0, explanation: "ignore_changes tells Terraform to exclude the listed attribute(s) from diff calculations after creation, so changes made outside Terraform (or intentionally left alone) don't keep showing up as drift." },
      { topic: "ignore_changes = all", stem: "What does setting 'lifecycle { ignore_changes = all }' do, as opposed to listing specific attribute names?", options: ["It ignores changes to every attribute on the resource after creation, effectively excluding the whole resource from update-in-place diffs (only true replacement-triggering changes still act)", "It's invalid syntax; ignore_changes must always list explicit attribute names", "It ignores changes only to attributes marked sensitive", "It behaves identically to prevent_destroy"], correct: 0, explanation: "The special value 'all' in ignore_changes applies the ignore behavior to every attribute on the resource, not just a specific list — useful when an external process manages most of the resource's fields." },
      { topic: "replace_triggered_by", stem: "What does the 'replace_triggered_by' lifecycle argument do?", options: ["Forces the resource to be replaced whenever a referenced value (another resource's attribute, or an entire other resource) changes, even if the resource's own arguments haven't changed", "Prevents a resource from ever being replaced, regardless of configuration changes", "Triggers a data source refresh whenever the resource is replaced", "Automatically retries a failed apply a fixed number of times"], correct: 0, explanation: "replace_triggered_by lets a resource be forced into replacement based on changes to something else entirely (e.g. a related resource or attribute), not just its own argument changes." },
      { topic: "replace_triggered_by Reference Types", stem: "What kinds of references can be listed in a resource's 'replace_triggered_by' argument?", options: ["References to attributes of other resources, or to entire other resources/instances (which then triggers replacement on any change to that resource, including creation or destruction)", "Only references to input variables, never to other resources", "Only references to data sources, never to managed resources", "Only literal string values representing version numbers"], correct: 0, explanation: "replace_triggered_by accepts references to specific resource attributes or to whole resources — referencing a whole resource means ANY change to that resource (including it being recreated) triggers replacement." },
      { topic: "Five Lifecycle Meta-Arguments", stem: "Which of these is NOT one of the standard lifecycle meta-arguments available on a resource block (as opposed to precondition/postcondition, which nest inside lifecycle but are their own construct)?", options: ["depends_on", "create_before_destroy", "prevent_destroy", "ignore_changes"], correct: 0, explanation: "depends_on is its own top-level meta-argument on the resource block, not nested inside lifecycle {}. The four core lifecycle settings are create_before_destroy, prevent_destroy, ignore_changes, and replace_triggered_by." },
      { topic: "depends_on Placement", stem: "Where is the 'depends_on' meta-argument declared relative to 'lifecycle { }'?", options: ["depends_on is a separate, top-level argument directly inside the resource (or module) block — it is NOT nested inside the lifecycle block", "depends_on must always be nested inside the lifecycle block, alongside prevent_destroy", "depends_on and lifecycle are mutually exclusive on the same resource", "depends_on only exists inside precondition blocks"], correct: 0, explanation: "depends_on sits directly in the resource/module block as its own meta-argument, distinct from the lifecycle {} block that holds create_before_destroy/prevent_destroy/ignore_changes/replace_triggered_by and the precondition/postcondition sub-blocks." },
      { topic: "precondition/postcondition Location", stem: "Where are 'precondition' and 'postcondition' blocks declared in a resource, data source, or output?", options: ["Nested inside that block's 'lifecycle { }' block, as their own precondition/postcondition sub-blocks", "As standalone top-level blocks alongside resource/data/output, not nested in anything", "Inside the variable block that supplies the resource's main argument", "Inside a separate 'conditions.tf' file that Terraform loads automatically"], correct: 0, explanation: "Custom conditions are sub-blocks of lifecycle { } — e.g. resource \"aws_instance\" \"x\" { lifecycle { precondition { condition = ..., error_message = ... } } } — not standalone top-level blocks." },
      { topic: "precondition Timing", stem: "When does a resource's 'precondition' block get evaluated, relative to that resource actually being created or updated?", options: ["After Terraform has generated a plan for the resource but before the resource is actually created/updated", "After the resource has already been created, verifying its resulting attributes", "Only during terraform destroy, never during apply", "Before Terraform even parses the resource's configuration"], correct: 0, explanation: "A precondition checks assumptions that must hold before Terraform proceeds to create or modify the resource — it runs post-plan but pre-apply for that resource." },
      { topic: "postcondition Timing", stem: "When does a resource's 'postcondition' block get evaluated?", options: ["After Terraform has applied the change (created/updated the resource), verifying a guarantee about the resulting state — for a data source, after it is read", "Before the resource's configuration is even validated", "At the same moment as the precondition, evaluated in parallel", "Only when the resource is being destroyed"], correct: 0, explanation: "Postconditions verify guarantees about the outcome — they run after the resource has actually been created/updated (or, for a data source, after it has been read)." },
      { topic: "precondition vs. Variable Validation", stem: "Why might you use a resource-level 'precondition' instead of (or in addition to) an input variable's 'validation' block?", options: ["A precondition can check assumptions involving resource/data source attributes only known after other parts of the configuration are evaluated, not just the raw input variable's own value", "Preconditions are strictly more limited than variable validation and can only check literal values", "Preconditions replace variable validation entirely as of Terraform 1.9 and validation blocks are deprecated", "There is no functional difference; they are two names for the exact same feature"], correct: 0, explanation: "Variable validation only has access to the variable's own value (plus, since 1.9, other referenceable objects known early); a resource's precondition can check conditions involving that specific resource's context, such as a related data source's attributes, at the point just before that resource is acted on." },
      { topic: "postcondition self_check Example", stem: "A postcondition on an aws_instance resource checks 'condition = self.private_ip != \"\"'. What does 'self' refer to here?", options: ["The resource the postcondition is attached to, letting the condition reference that resource's own resulting attributes", "The Terraform CLI's current working directory", "The parent module calling this resource", "The previous resource in the configuration file, regardless of relationship"], correct: 0, explanation: "Inside a resource's own postcondition, 'self' refers to that resource itself, allowing the condition to check attributes of the resource that was just created/updated." },
    ],
  },
  {
    name: "Ephemeral Values & Write-Only Arguments",
    subject: "Ephemeral Values",
    sourceNote: SRC_EPHEMERAL,
    questions: [
      { topic: "Ephemeral Values Definition", stem: "What is an 'ephemeral value' in Terraform, at its core?", options: ["A value that Terraform uses only during the current operation and never persists to state or plan files", "Any value that is marked sensitive = true", "A value that only exists for resources using the 'random' provider", "A deprecated term for a 'local' value that has since been renamed"], correct: 0, explanation: "Ephemeral values are Terraform's mechanism for handling data (like short-lived credentials) that should never be written to state or plan files, existing only for the duration of the operation that needs them." },
      { topic: "Ephemeral Resource Block Syntax", stem: "What is the correct syntax for declaring an ephemeral resource?", options: ["ephemeral \"<TYPE>\" \"<LABEL>\" { ... }", "resource \"<TYPE>\" \"<LABEL>\" { ephemeral = true ... }", "data \"<TYPE>\" \"<LABEL>\" { ephemeral { } }", "ephemeral_resource \"<LABEL>\" { type = \"<TYPE>\" }"], correct: 0, explanation: "Ephemeral resources use their own top-level block type, 'ephemeral', with the same two-label syntax pattern as resource and data blocks: ephemeral \"<TYPE>\" \"<LABEL>\" { }." },
      { topic: "Who Defines Ephemeral Resource Types", stem: "Where do the available ephemeral resource TYPEs (e.g. an ephemeral 'random_password' or an ephemeral secret-fetching resource) come from?", options: ["Provider developers define which ephemeral resource types their provider exposes — they come from providers, not from Terraform core", "They are a fixed, built-in list hard-coded into Terraform core, the same for every provider", "They can only be defined inside a user's own root module using a special 'ephemeral_type' block", "Every existing managed resource type automatically also has an ephemeral equivalent"], correct: 0, explanation: "Like managed resources and data sources, ephemeral resource types are defined by provider developers — a provider must explicitly implement an ephemeral resource type for it to be usable." },
      { topic: "Referencing an Ephemeral Resource", stem: "How do you reference an attribute of a declared ephemeral resource, e.g. one named 'db_password' of type 'random_password'?", options: ["ephemeral.random_password.db_password.result", "random_password.db_password.result", "data.ephemeral.random_password.db_password.result", "var.ephemeral.random_password.db_password"], correct: 0, explanation: "Ephemeral resources are referenced with the pattern ephemeral.<TYPE>.<LABEL>.<ATTRIBUTE> — analogous to how a data source is referenced as data.<TYPE>.<LABEL>.<ATTRIBUTE>." },
      { topic: "Ephemeral Resource State Storage", stem: "Are ephemeral resources ever recorded in Terraform's state file?", options: ["No — Terraform never stores ephemeral resource information in state or plan files, by design", "Yes, but only their attribute names, never their values", "Yes, they are stored in state exactly like managed resources", "Only if the ephemeral resource's provider explicitly opts into state storage"], correct: 0, explanation: "Ephemeral resources are fundamentally defined by NOT being persisted — Terraform doesn't record them in state or plan files at all, which is the entire point of the feature." },
      { topic: "Ephemeral Resource Lifecycle", stem: "What is the documented lifecycle of an ephemeral resource across a Terraform operation?", options: ["Open (when Terraform first needs its result), an optional periodic renew (if needed longer than the remote system's expiration), then close (once dependent providers finish using it for that run phase)", "Create, read, update, delete — identical to a managed resource's CRUD lifecycle", "Plan, apply, destroy — identical to the root module's own lifecycle", "Fetch once at terraform init and cache for the lifetime of the working directory"], correct: 0, explanation: "Ephemeral resources follow their own open/renew/close lifecycle: Terraform opens them when their result is needed, may ask the provider to periodically renew access, and closes them once the current run phase's dependent work is done." },
      { topic: "Write-Only Arguments Introduction", stem: "In which Terraform version were write-only arguments on managed resources introduced?", options: ["Terraform 1.11", "Terraform 1.0", "Terraform 1.5", "Terraform 1.9"], correct: 0, explanation: "Write-only arguments (accepting ephemeral or non-ephemeral values on ordinary managed resources) were introduced in Terraform 1.11, building on 1.10's ephemeral values foundation." },
      { topic: "Write-Only Argument State Behavior", stem: "What value does Terraform record in state for a write-only argument (e.g. a provider-defined 'password_wo' attribute)?", options: ["null — the prior state, planned state, and final state values for a write-only argument are always null", "The literal value that was supplied, same as any other argument", "A one-way hash of the value, for drift-detection purposes only", "The value is stored in state but encrypted with a Terraform-managed key"], correct: 0, explanation: "Write-only arguments are defined so that their prior, planned, and final state values are always null — the actual value is sent to the provider for the operation and never persisted." },
      { topic: "Why Write-Only Args Need a Companion Trigger", stem: "Since a write-only argument's state value is always null, why is it conventionally paired with a companion argument like 'password_wo_version'?", options: ["Because a null-vs-null comparison across plans can never itself indicate a change, so a separate trigger (e.g. bumping a version number) is needed to signal the provider that the write-only value should be re-applied", "Because Terraform requires every write-only argument to have a matching '_version' argument as a hard syntax rule enforced by core, unrelated to diffing", "Because the companion argument stores an encrypted backup of the write-only value", "Because write-only arguments are otherwise limited to being set exactly once, at resource creation, with no other way to update them"], correct: 0, explanation: "Because write-only values never show up as a state diff on their own (always null-to-null), providers rely on a companion argument the practitioner changes deliberately (like a version counter) to know when to re-send/re-apply the write-only value." },
      { topic: "Write-Only Args and Ephemeral Values", stem: "Can a write-only argument accept a non-ephemeral (ordinary, persistent) value, or must its source always be an ephemeral value/resource?", options: ["A write-only argument can accept either an ephemeral value or an ordinary non-ephemeral value — ephemeral-compatibility is what makes it able to accept ephemeral values, not a requirement that it always does", "It must always be sourced from an ephemeral resource or ephemeral-marked variable; plain literal strings are rejected", "It can only ever be a literal hardcoded value in configuration, never a reference", "Write-only arguments and ephemeral values are entirely unrelated features with no overlap"], correct: 0, explanation: "Write-only arguments are compatible with ephemeral values (which is their main use case for secrets) but aren't restricted to them — they can also just take an ordinary non-ephemeral value if that's what's supplied." },
      { topic: "Write-Only Argument Restrictions", stem: "Which of the following is a documented restriction on write-only arguments?", options: ["They cannot be used with set attributes, set nested attributes, or set nested blocks", "They cannot be used inside any resource that also has a lifecycle block", "They cannot be referenced by any other resource in the same configuration", "They can only be declared on data sources, never on managed resources"], correct: 0, explanation: "Write-only arguments are explicitly documented as incompatible with set-based attribute/block types (set attributes, set nested attributes, set nested blocks), among other structural restrictions." },
      { topic: "ephemeralasnull() Purpose", stem: "What does the 'ephemeralasnull()' function do?", options: ["It accepts an ephemeral value (including one nested inside an object) and returns null, effectively stripping out ephemeral-ness by discarding the value", "It converts an ephemeral resource into a permanently persisted resource in state", "It marks an otherwise-ordinary value as ephemeral going forward", "It renews an ephemeral resource's lease without closing it"], correct: 0, explanation: "ephemeralasnull() takes an ephemeral value and returns null, which is useful when you need to satisfy a context that can't accept an ephemeral value but where null is an acceptable substitute — it nullifies rather than 'unwraps' the ephemeral value." },
      { topic: "ephemeralasnull() Version", stem: "Since which Terraform version has the 'ephemeralasnull()' function been available?", options: ["Terraform 1.10", "Terraform 1.11", "Terraform 1.5", "Terraform 1.0 — it has always existed"], correct: 0, explanation: "ephemeralasnull() shipped alongside the initial ephemeral values feature in Terraform 1.10, ahead of write-only arguments' arrival in 1.11." },
      { topic: "Ephemeral Values in Outputs", stem: "Can an ephemeral value be assigned directly to a root module's regular output value?", options: ["No — an ephemeral value cannot flow into a normal output; the output itself must be declared ephemeral (or the value nullified, e.g. via ephemeralasnull()) for that to be allowed", "Yes — any output can freely hold an ephemeral value with no special declaration needed", "Yes, but only outputs of type string can hold ephemeral values", "Ephemeral values are automatically converted to sensitive outputs, never rejected"], correct: 0, explanation: "Ephemeral values can't leak into persisted state via a normal output. An output must itself be marked ephemeral to legitimately hold one (or the value must be stripped down to null first)." },
    ],
  },
];
