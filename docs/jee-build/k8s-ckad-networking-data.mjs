// CKAD Practice: Services & Networking
// Fills a confirmed gap: the CKAD official curriculum's "Services &
// Networking" domain (~20% per current CNCF exam guide, re-verified via web
// search 2026-09-04 — domains: Application Environment/Configuration/
// Security ~25%, Application Design & Build ~20%, Application Deployment
// ~20%, Services & Networking ~20%, Application Observability &
// Maintenance ~15%) has ZERO coverage across the 2 existing live CKAD
// papers (ckad-application-design-deployment covers Design/Build/Deployment;
// ckad-configuration-security-observability covers Config/Security/
// Observability, including one NetworkPolicy sub-thread from the SECURITY
// angle only — namespaceSelector/podSelector combos, default-deny, egress
// DNS rule). This paper is deliberately scoped to the application-developer
// framing CKAD's blueprint uses for this domain (Services, Ingress,
// NetworkPolicy from an app-dev perspective, DNS resolution, multi-container
// Pod networking) and does not repeat the CKA cluster-admin angle on the
// same topics already covered in cka-workloads-networking-troubleshooting
// (ClusterIP/NodePort/LoadBalancer basics, Ingress-requires-a-controller,
// NetworkPolicy default-deny fact, headless Service definition, DNS FQDN
// format, Service selector-mismatch symptom) — those exact facts are
// referenced only where a genuinely different, app-dev-scoped question could
// be built around them without duplicating the stem.
//
// Facts verified via web search against current Kubernetes docs (2026-09-04):
//  - Default Service type when 'type' is omitted is ClusterIP.
//  - Default NodePort allocation range is 30000-32767.
//  - Ingress TLS termination uses a Secret of type kubernetes.io/tls.
//  - pathType values (Exact/Prefix/ImplementationSpecific) and
//    ingressClassName's role in selecting an IngressClass/controller are
//    core (stable) Ingress API fields, not annotations.
//  - EndpointSlice is the stable, scalable successor to the legacy
//    Endpoints object (stable since Kubernetes v1.21).
//  - NetworkPolicy enforcement depends entirely on the cluster's CNI plugin
//    supporting it; Kubernetes core does not enforce NetworkPolicy itself.
// Current CKAD exam guide (per CNCF materials as of this build) targets
// Kubernetes v1.35 — no version-specific behavior below is newer than that.

export const HEADER_LABEL = "CKAD Practice: Services & Networking";

export const PAPER = {
  slug: "ckad-services-networking",
  title: "CKAD Practice: Services & Networking",
  examType: "kubernetes",
  focusTags: ["CKAD", "Services", "Ingress", "NetworkPolicy", "DNS"],
  questionCount: 37,
  difficulty: "standard",
  sortOrder: 6,
  timeLimitMinutes: 60,
  positiveMarks: 1,
  negativeMarks: 0,
};

const SRC_NET =
  "Services & Networking — authored from kubernetes.io documentation (Services, Ingress, Network Policies, DNS for Services and Pods) and the current CNCF CKAD exam curriculum's Services & Networking domain, framed from the application-developer perspective the CKAD blueprint uses; verified via web search against current Kubernetes docs, not copied from any official CNCF exam material.";

export const SECTIONS = [
  {
    name: "Services",
    subject: "Services & Networking",
    sourceNote: SRC_NET,
    questions: [
      { topic: "Service Types", stem: "Which Service type is the default when 'type' is omitted from a Service spec?", options: ["ClusterIP", "NodePort", "LoadBalancer", "ExternalName"], correct: 0, explanation: "ClusterIP is the default Service type, exposing the Service only on an internal cluster IP reachable from within the cluster." },
      { topic: "ExternalName Service", stem: "What does a Service of type ExternalName actually do?", options: ["It returns a CNAME record pointing to an external DNS name given in spec.externalName — no proxying, selector, or cluster IP is involved", "It load-balances traffic across Pods running in a different cluster", "It's functionally identical to ClusterIP but restricted to a specific namespace", "It automatically provisions a cloud load balancer for an external endpoint"], correct: 0, explanation: "ExternalName Services work purely at the DNS level, returning a CNAME to spec.externalName. There's no selector, no Endpoints, and no actual traffic proxying by kube-proxy." },
      { topic: "Service Without Selector", stem: "A Service is created with no 'selector' field. As an application developer, when is this pattern useful, and what do you have to do to make it work?", options: ["To route traffic to an endpoint outside normal Pod selection (e.g. an external database); you must manually create a matching Endpoints or EndpointSlice object yourself since there are no Pods to auto-populate it", "It's invalid — every Service must have a selector", "It automatically becomes a headless Service with no further action needed", "It only works for Services of type LoadBalancer"], correct: 0, explanation: "A selector-less Service doesn't get an auto-managed Endpoints object; the developer must create one manually (matching the Service's name) pointing at the actual backend IPs/ports, commonly used to front an external resource." },
      { topic: "Multi-Port Services", stem: "A Service exposes two ports on the same backend Pods. Why is it good practice to name each port (in both the container spec and the Service spec) rather than referencing them only by number?", options: ["Named ports make multi-port Service/Pod specs self-documenting and let other objects reference the port by name instead of repeating raw numbers everywhere", "Kubernetes requires named ports for any Service exposing more than one port — unnamed multi-port Services are rejected", "Named ports are required for TLS termination", "Named ports let you skip specifying targetPort entirely"], correct: 0, explanation: "Naming ports is a readability/maintainability convention, not a hard requirement — a multi-port Service works fine with only numeric ports, but named ports make the intent clearer and are referenceable by name." },
      { topic: "Service Port Fields", stem: "In a Service spec's ports entry, what's the distinction between 'port' and 'targetPort'?", options: ["'port' is the port the Service itself listens on (what clients use to reach it); 'targetPort' is the port on the backend Pod's container that traffic is actually forwarded to — they can differ", "'port' is for TCP and 'targetPort' is for UDP", "'targetPort' is only used for NodePort Services", "They must always be set to the same numeric value"], correct: 0, explanation: "'port' is the Service's own listening port; 'targetPort' is where traffic is actually sent on the backend Pod, and the two are commonly different (e.g. Service port 80 forwarding to container port 8080)." },
      { topic: "NodePort Range", stem: "By default, what port range does Kubernetes allocate NodePort values from?", options: ["30000-32767", "1024-2048", "8000-9000", "443-8443"], correct: 0, explanation: "The default NodePort range is 30000-32767 (configurable cluster-wide via the API server's --service-node-port-range flag)." },
      { topic: "sessionAffinity", stem: "Setting a Service's sessionAffinity to ClientIP does what?", options: ["Routes repeated requests from the same client IP to the same backend Pod (within a timeout window), instead of the default distribution across all matching Pods", "Restricts the Service to only accept traffic from a single, hardcoded client IP", "Forces all traffic through a single node's IP", "Disables load balancing entirely for that Service, functioning like a headless Service"], correct: 0, explanation: "sessionAffinity: ClientIP makes kube-proxy stick a given source IP to the same backend Pod for a configurable timeout, rather than distributing every request independently." },
      { topic: "Readiness and Service Endpoints", stem: "A Pod's labels correctly match a Service's selector, but the Pod is failing its readinessProbe. What's the effect on that Service?", options: ["The Pod is excluded from the Service's Endpoints/EndpointSlice until it passes readiness again — traffic is not sent to a not-Ready Pod even though its labels match", "The Service is deleted automatically", "Traffic is still sent to it, since readiness only affects rolling updates, not Services", "The Pod is automatically restarted by the Service"], correct: 0, explanation: "Endpoints/EndpointSlice membership depends on both label match AND readiness — a Pod that matches the selector but fails readiness is removed from the routable set until it becomes Ready again." },
      { topic: "kubectl expose", stem: "Running 'kubectl expose deployment my-app --port=80 --target-port=8080' creates what?", options: ["A new Service (ClusterIP by default) that routes port 80 to port 8080 on the Deployment's Pods, using the Deployment's Pod-template labels as its selector", "A new Ingress resource routing to the Deployment", "A new Deployment identical to my-app but exposed publicly", "A NetworkPolicy allowing external access to the Deployment"], correct: 0, explanation: "kubectl expose generates a Service (ClusterIP unless --type is given) whose selector matches the source Deployment's Pod labels, forwarding the given port to targetPort on those Pods." },
      { topic: "Legacy Service Discovery via Env Vars", stem: "Kubernetes injects Service-discovery environment variables (like MY_SVC_SERVICE_HOST) into Pods, in addition to DNS. What's the well-known limitation of relying on these env vars for service discovery, from an application-developer's perspective?", options: ["They're only populated for Services that already existed at the time the consuming Pod was created — a Service created afterward won't retroactively appear in the env vars of already-running Pods", "They only work for Services of type LoadBalancer", "They expose the full Service YAML as JSON in a single variable", "They're deprecated and no longer injected in any current Kubernetes version"], correct: 0, explanation: "Service env vars are injected only at Pod creation time based on Services that existed then — this ordering limitation is exactly why DNS-based discovery is generally preferred by application developers." },
      { topic: "kubectl port-forward", stem: "As an app developer, what does 'kubectl port-forward svc/my-service 8080:80' let you do?", options: ["Temporarily forward a local port on your machine to the Service's port for ad-hoc local access/debugging, without exposing the Service externally via NodePort/LoadBalancer/Ingress", "Permanently change the Service's type to NodePort", "Create a new Ingress rule bound to port 8080", "Rewrite the Service's selector to match a different Deployment"], correct: 0, explanation: "port-forward opens a temporary tunnel from a local port to a Service/Pod's port for the current session only — it doesn't modify the Service or expose it to anything outside that tunnel." },
      { topic: "EndpointSlice", stem: "What does the EndpointSlice API provide as the modern, stable successor to the original Endpoints object?", options: ["A more scalable way to track a Service's backing Pod IPs/ports, splitting large endpoint sets across multiple slices rather than one unbounded Endpoints object, with support for multiple IP families and extra topology info", "A way to bypass kube-proxy entirely", "A new mechanism that replaces Services altogether", "A CRD that only NetworkPolicy consumes"], correct: 0, explanation: "EndpointSlice (stable since Kubernetes v1.21) improves on the single, unbounded Endpoints object by sharding large backend sets across multiple slices and supporting dual-stack IPs and topology hints." },
      { topic: "Service Selector Labels", stem: "For a Service to route traffic to a set of Pods at all, what must be true, at minimum?", options: ["The Service's spec.selector key/value pairs must match labels present on the target Pods, and those Pods must expose the matching containerPort", "The Pods must be in a Deployment specifically — bare Pods can never be selected by a Service", "The Service and Pods must share the exact same name", "The Pods must be annotated with the Service's UID"], correct: 0, explanation: "Service-to-Pod routing is purely label-based via spec.selector; the Service has no awareness of what controller (or none) created the matching Pods." },
    ],
  },
  {
    name: "Ingress",
    subject: "Services & Networking",
    sourceNote: SRC_NET,
    questions: [
      { topic: "Ingress Host-Based Routing", stem: "A single Ingress resource defines rules for host: api.example.com and host: app.example.com, each routing to different Services. What does this express?", options: ["Host-based routing: requests are routed to different backend Services depending on the Host header, all through one Ingress controller/load balancer", "It's invalid; an Ingress may only define one host per resource", "Both hostnames must resolve to different Ingress controllers", "It creates two separate LoadBalancer Services automatically"], correct: 0, explanation: "An Ingress resource can list multiple host rules, each with its own routing, all served through the same Ingress controller and typically the same external IP/load balancer." },
      { topic: "Ingress Path-Based Routing & pathType", stem: "An Ingress rule sets path: /api with pathType: Prefix. What does pathType: Prefix mean here, as distinct from Exact?", options: ["The rule matches any request path that starts with /api (e.g. /api/users), not just the literal string /api; Exact would require the path to match precisely", "Prefix means the rule only matches when the path has no trailing segments at all", "pathType has no effect on matching; it's purely cosmetic metadata", "Prefix restricts matching to HTTPS requests only"], correct: 0, explanation: "pathType: Prefix does element-wise prefix matching on path segments (so /api matches /api/users), while Exact requires the request path to match the given path string exactly." },
      { topic: "Ingress Default Backend", stem: "A request arrives at the Ingress controller that doesn't match any rule's host/path in any Ingress resource. What typically handles it, if configured?", options: ["The Ingress controller's default backend (or a defaultBackend field on an Ingress), which serves a fallback response (commonly a 404) for otherwise-unmatched requests", "The request is automatically routed to the first Service alphabetically", "The apiserver itself intercepts and answers the request", "The request is silently dropped and cannot generate a response"], correct: 0, explanation: "A defaultBackend (either configured on the Ingress controller itself or via an Ingress's spec.defaultBackend) catches requests that don't match any defined rule." },
      { topic: "Ingress TLS", stem: "How does an Ingress resource terminate TLS for a given host?", options: ["By referencing a Secret of type kubernetes.io/tls (containing tls.crt/tls.key) in the Ingress's spec.tls block for that host — the Ingress controller uses it to terminate HTTPS", "TLS termination is handled automatically without any Secret, purely from the Service's port number", "By setting a NetworkPolicy that allows port 443", "Ingress resources cannot terminate TLS; that always has to happen at the Service"], correct: 0, explanation: "TLS termination at the Ingress layer is configured via spec.tls, referencing a kubernetes.io/tls Secret holding the certificate/key the controller presents for that host." },
      { topic: "ingressClassName", stem: "A cluster has two different Ingress controllers installed. How does an Ingress resource specify which controller should handle it?", options: ["Via spec.ingressClassName, referencing the name of an IngressClass object associated with the desired controller", "Via the Service's type field", "Ingress resources are always handled by every installed controller simultaneously", "By naming the Ingress resource identically to the controller's Deployment"], correct: 0, explanation: "spec.ingressClassName points at an IngressClass resource, which in turn identifies the controller responsible for implementing that Ingress's rules — this is how multiple controllers coexist cleanly." },
      { topic: "Ingress Requires a Controller", stem: "You create Ingress resources in a cluster where no Ingress controller has been installed. What happens?", options: ["The Ingress objects are accepted and stored by the API server, but nothing actually implements the routing — no traffic is proxied per those rules until a controller is running", "The API server rejects Ingress creation outright", "Kubernetes automatically installs a default Ingress controller", "The Ingress rules are silently converted into NodePort Services"], correct: 0, explanation: "Ingress is just an API object describing desired routing; it has zero effect without a running controller watching and implementing those rules." },
      { topic: "Ingress vs Service", stem: "From an application developer's perspective, why would you use an Ingress in front of a Service rather than just making the Service a LoadBalancer directly?", options: ["Ingress lets you consolidate host/path-based routing for many Services behind a single external entry point/IP, plus centralize concerns like TLS termination — rather than provisioning a separate external load balancer per Service", "Ingress is required before a Service can have any Pods at all", "A Service can never receive external traffic without an Ingress", "Ingress replaces the need for Pod labels"], correct: 0, explanation: "Ingress's main value is consolidating routing (and often TLS) for many backend Services behind one external endpoint, avoiding a dedicated cloud load balancer per Service." },
      { topic: "Ingress Annotations", stem: "Ingress controllers commonly support behavior (like URL rewriting, e.g. nginx.ingress.kubernetes.io/rewrite-target) through which mechanism, since it's not part of the core Ingress API spec?", options: ["Controller-specific annotations on the Ingress object — these are implementation-specific extensions, not portable across different Ingress controllers", "A required field called spec.rewriteRules present on every Ingress", "A separate CRD that must be manually installed for every annotation", "Environment variables set on the Ingress controller's own Pods only"], correct: 0, explanation: "Behavior beyond the core Ingress spec (host/path/TLS routing) is typically exposed via controller-specific annotations, which don't transfer if you switch Ingress controllers." },
      { topic: "Same Host+Path in Two Ingress Resources", stem: "Two separate Ingress resources both define the exact same host and path, pointing to different backend Services. What can you generally expect?", options: ["The behavior is undefined/controller-specific — most controllers pick one deterministically (e.g. oldest resource), but this is a misconfiguration you should avoid rather than rely on", "Kubernetes automatically load-balances 50/50 between both backend Services", "The API server rejects the second Ingress at creation time in every case", "Both rules are always merged into a single route with no ambiguity"], correct: 0, explanation: "The core Ingress API doesn't define a merge/conflict-resolution rule for overlapping host+path across resources; behavior is left to the specific controller's implementation, so it should be treated as a misconfiguration." },
      { topic: "Ingress Resource Scope", stem: "Does creating an Ingress resource, by itself, expose your application outside the cluster?", options: ["Not by itself — the Ingress resource just declares routing rules; an actual Ingress controller (running as Pods, typically fronted by a LoadBalancer or NodePort Service) must be deployed and watching for it to take effect", "Yes, Ingress objects always provision their own dedicated external IP automatically with zero other components needed", "No — Ingress can only route traffic that originates inside the cluster", "Yes, but only for gRPC traffic"], correct: 0, explanation: "An Ingress object is declarative-only; actual external exposure comes from the controller implementing it, which is itself typically fronted by its own Service." },
    ],
  },
  {
    name: "NetworkPolicy, DNS & Pod-to-Pod Networking",
    subject: "Services & Networking",
    sourceNote: SRC_NET,
    questions: [
      { topic: "NetworkPolicy policyTypes", stem: "A NetworkPolicy's spec includes only an 'egress' block and no 'ingress' block, but doesn't explicitly set policyTypes. What's the risk of omitting policyTypes here?", options: ["If policyTypes isn't set explicitly, Kubernetes infers it from which of ingress/egress blocks are present — but it's best practice to set it explicitly, since relying on inference can produce a policy that doesn't restrict the traffic direction you intended", "The NetworkPolicy is rejected by the API server for missing a required field", "policyTypes has no effect on behavior in any case; it's purely documentation", "Without policyTypes, the policy defaults to blocking all traffic in both directions regardless of the ingress/egress blocks present"], correct: 0, explanation: "policyTypes is inferred from which blocks are present if omitted, but this inference can silently miss your intent (e.g. wanting an ingress-only or egress-only policy) — explicit is safer." },
      { topic: "App-Scoped NetworkPolicy", stem: "As an application developer packaging a NetworkPolicy alongside your own microservice's manifests, you want to allow only Pods belonging to your own application (label app: checkout) to receive traffic from Pods labeled app: frontend, in the same namespace. Which field expresses the 'who is allowed in' condition?", options: ["spec.ingress[].from[].podSelector, matching on app: frontend, combined with spec.podSelector on the policy itself selecting app: checkout", "spec.egress[].to[].podSelector on a policy that selects app: frontend", "metadata.labels on the NetworkPolicy object itself", "spec.serviceAccountName referencing the frontend Pods' ServiceAccount"], correct: 0, explanation: "The policy's own spec.podSelector picks which Pods it protects (app: checkout); its ingress[].from[].podSelector picks which source Pods are allowed in (app: frontend)." },
      { topic: "NetworkPolicy Egress-Only Restriction", stem: "You want a NetworkPolicy that restricts only outbound (egress) traffic from your Pods, while leaving inbound traffic completely unaffected by this policy. What must you make sure of?", options: ["Set policyTypes: [Egress] only (omitting Ingress), and include only an egress block — leaving Ingress out of policyTypes avoids also imposing a default-deny on inbound traffic", "Set policyTypes: [Ingress, Egress] always, regardless of intent", "NetworkPolicy cannot restrict egress traffic under any circumstances — only ingress", "Add an ingress rule with an empty 'from' to explicitly allow all inbound"], correct: 0, explanation: "Explicitly scoping policyTypes to [Egress] only prevents the policy from also becoming a default-deny for ingress traffic to the selected Pods." },
      { topic: "NetworkPolicy Port Restriction", stem: "A NetworkPolicy's ingress rule includes a 'ports' field specifying protocol: TCP and port: 8080. What's the effect?", options: ["Only traffic matching that specific protocol/port combination is allowed by that rule, even if the 'from' peer selector otherwise matches — other ports to the same Pods remain governed by whatever other rules (or the default) apply", "The port field is ignored; NetworkPolicy can only filter by IP/label, never by port", "It opens all ports on the selected Pods once any port is listed", "It applies only to UDP traffic on that port, never TCP"], correct: 0, explanation: "NetworkPolicy rules can scope allowed traffic down to specific protocol/port pairs in addition to peer selection — a rule allowing TCP/8080 doesn't implicitly allow other ports." },
      { topic: "CNI Enforcement of NetworkPolicy", stem: "You create a technically well-formed NetworkPolicy, but traffic still isn't being restricted as expected. What's a common root cause worth checking first?", options: ["Kubernetes itself doesn't enforce NetworkPolicy — it's up to the cluster's CNI plugin to implement it (e.g. Calico, Cilium); if the installed CNI doesn't support NetworkPolicy, the objects are accepted but have no actual effect", "NetworkPolicy objects require a paid Kubernetes distribution to function at all", "NetworkPolicy only takes effect after a full cluster restart", "NetworkPolicy can only be created by a ClusterRole with wildcard permissions"], correct: 0, explanation: "NetworkPolicy is an API contract enforced by the CNI, not the control plane itself — a non-enforcing CNI silently accepts the objects without applying any restriction." },
      { topic: "Cluster DNS FQDN", stem: "From inside a Pod, what's the standard fully-qualified DNS name to reach a Service named 'payments' in the 'billing' namespace?", options: ["payments.billing.svc.cluster.local (with 'cluster.local' being the default cluster domain, which can be configured differently in some clusters)", "payments.billing.pod.local", "svc.billing.payments.cluster", "billing-payments.svc"], correct: 0, explanation: "Cluster DNS follows <service>.<namespace>.svc.<cluster-domain>, with cluster.local as the conventional default cluster domain." },
      { topic: "Cross-Namespace Service Calls", stem: "An application in the 'frontend' namespace wants to call a Service named 'payments' in the 'billing' namespace. Simply using the hostname 'payments' will not resolve correctly from 'frontend'. Why, and what's the fix?", options: ["A bare Service name only resolves within its own namespace by default search-domain expansion; the caller must use the namespace-qualified name payments.billing (or the full FQDN) to resolve across namespaces", "Cross-namespace Service calls are blocked by Kubernetes unless a NetworkPolicy explicitly allows DNS", "Service names are globally unique across namespaces, so 'payments' alone should always work identically everywhere", "DNS only works within the default namespace; all others require an Ingress"], correct: 0, explanation: "The default Pod DNS search path resolves a bare name within the Pod's own namespace first; reaching a Service in another namespace requires at least the namespace-qualified form." },
      { topic: "Per-Pod DNS for StatefulSets", stem: "For a Pod managed by a StatefulSet behind a headless Service named 'db', how can another Pod address one SPECIFIC ordinal Pod (not just any Pod behind the Service) by DNS name?", options: ["Via its stable per-Pod DNS record: <pod-name>.<headless-service-name>.<namespace>.svc.cluster.local (e.g. db-0.db.default.svc.cluster.local)", "By using the Pod's ephemeral IP address only — no stable per-Pod DNS name exists", "By appending the Pod's UID to the Service's DNS name", "Individual Pods behind any Service are never individually addressable by DNS"], correct: 0, explanation: "A headless Service backing a StatefulSet publishes individual DNS records per Pod, addressed as <pod-name>.<service-name>.<namespace>.svc.cluster.local, letting callers target one specific ordinal." },
      { topic: "Custom Pod DNS Config", stem: "An application needs to query a custom, non-cluster DNS nameserver instead of the cluster's default resolution chain. Which Pod spec fields support this?", options: ["dnsPolicy: 'None' combined with an explicit dnsConfig block specifying nameservers/searches/options", "Only an environment variable named DNS_SERVER, which Kubernetes reads automatically", "This isn't configurable per-Pod; it requires changing the cluster's CoreDNS ConfigMap for every Pod cluster-wide", "securityContext.dnsCapabilities"], correct: 0, explanation: "dnsPolicy: None disables the default DNS chain entirely, requiring dnsConfig to fully specify nameservers/search domains/options for that Pod." },
      { topic: "Multi-Container Pod Networking", stem: "Two containers in the same Pod both want to talk to each other. What's true about how they can reach one another?", options: ["They share the same network namespace (and thus the same Pod IP), so one container can reach the other via localhost:<port> — no Service or DNS lookup is needed between containers in the same Pod", "They must use the Pod's Service DNS name to reach each other, exactly as an external client would", "They can only communicate via a shared volume, never over the network", "Each container in a Pod gets its own separate IP address"], correct: 0, explanation: "All containers in a Pod share one network namespace and Pod IP; they communicate over localhost without needing a Service, DNS, or any cluster networking layer." },
      { topic: "Port Conflicts Within a Pod", stem: "Two containers within the same Pod both try to configure their process to listen on TCP port 8080. What happens?", options: ["This causes a real conflict, since containers in a Pod share one network namespace/IP — only one process can actually bind port 8080 at a time; the second container's listener will fail to bind", "Kubernetes automatically remaps one of them to a different port transparently", "Each container gets an isolated view of port 8080, so there's no conflict", "This is only a problem if a Service also targets port 8080"], correct: 0, explanation: "Because containers in a Pod share the network namespace, port binding is a genuine shared resource — two containers cannot both successfully bind the same port." },
      { topic: "Sidecar-to-Main-Container Networking", stem: "A sidecar container in a Pod (e.g. a log shipper or proxy) needs to reach the main application container running on port 8080 in the same Pod. What's the standard way to do this?", options: ["Connect to localhost:8080 (or 127.0.0.1:8080), since both containers share the Pod's network namespace", "Look up the main container by its container name via cluster DNS", "Create a dedicated ClusterIP Service just for intra-Pod communication", "Use the Pod's external LoadBalancer IP"], correct: 0, explanation: "Same-Pod containers always reach each other over localhost — no Service, DNS entry, or external routing is involved for intra-Pod communication." },
      { topic: "NetworkPolicy Default Namespace Behavior (App-Dev Angle)", stem: "You're deploying a new microservice into a namespace that already enforces default-deny NetworkPolicies for ingress. As the application's developer, what do you need to add for your own Service to actually receive traffic from its intended callers?", options: ["A NetworkPolicy selecting your Pods (via podSelector) with an ingress rule explicitly allowing traffic from the specific caller Pods/namespaces you expect — default-deny means nothing gets in until you say so", "Nothing — default-deny only applies to egress, never ingress, so your Service works immediately", "Change your Service's type to LoadBalancer to bypass NetworkPolicy entirely", "Disable NetworkPolicy enforcement cluster-wide from within your own namespace's manifests"], correct: 0, explanation: "Under a default-deny-ingress posture, every workload must ship its own allow-list NetworkPolicy; there's no per-Service opt-out, and Service type has no bearing on NetworkPolicy enforcement." },
      { topic: "DNS Resolution Failure First Check (App-Dev Angle)", stem: "Your application Pod can reach other Pods by IP but fails to resolve any Service by name. From your own Pod's manifest/config, what's a quick first thing to verify before escalating to a cluster-wide CoreDNS issue?", options: ["That the Pod's dnsPolicy hasn't been overridden to something like 'Default' (which uses the node's DNS instead of cluster DNS), or that a custom dnsConfig isn't misconfigured", "That the Pod's image is based on Alpine Linux specifically", "That the Pod has a PersistentVolumeClaim attached", "That the Service's type is set to ExternalName"], correct: 0, explanation: "A per-Pod dnsPolicy override (or a broken custom dnsConfig) is a self-contained, Pod-manifest-level cause that's worth ruling out before assuming a cluster-wide CoreDNS failure." },
    ],
  },
];
