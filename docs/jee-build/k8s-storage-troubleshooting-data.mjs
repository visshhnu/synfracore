// CKA Practice: Storage & Advanced Troubleshooting
// Fills a confirmed gap: the CKA official curriculum's dedicated "Storage"
// domain (~10% per current CNCF exam guide, re-verified via web search
// 2026-09-04 against multiple current CKA exam-guide sources — domains:
// Cluster Architecture/Installation/Configuration ~25%, Workloads &
// Scheduling ~15%, Storage ~10%, Services & Networking ~20%,
// Troubleshooting ~30%) has ZERO coverage across the 2 existing live CKA
// papers (cka-cluster-architecture-administration,
// cka-workloads-networking-troubleshooting). Those 2 papers do touch PVC/PV
// "purpose" and StorageClass "role" at a single-question, definitional
// level (see cka-workloads-networking-troubleshooting Q36/Q37) — this paper
// goes deep on Storage domain specifics those only gestured at (access
// modes incl. ReadWriteOncePod, volumeMode, reclaim policies, binding
// mechanics, dynamic provisioning fields, StatefulSet PVC retention) and
// separately deepens Troubleshooting with scenarios beyond what the
// existing (networking-bundled) troubleshooting paper covers.
//
// API/behavior facts below verified via web search against current
// Kubernetes docs and CNCF exam-guide sources (2026-09-04), not from
// memory alone:
//  - storage.k8s.io/v1 is the StorageClass API group/version.
//  - ReadWriteOncePod graduated to GA/stable in Kubernetes v1.29 (CSI-only).
//  - StatefulSet persistentVolumeClaimRetentionPolicy (whenScaled/whenDeleted)
//    graduated to stable in Kubernetes v1.32; PVCs are retained by default
//    when this field isn't set.
//  - Default reclaimPolicy for dynamically-provisioned PVs is Delete;
//    volumeBindingMode WaitForFirstConsumer delays binding until a
//    consuming Pod is scheduled (topology-aware provisioning).
// Current CKA exam guide (per CNCF materials as of this build) targets
// Kubernetes v1.35 — no version-specific behavior below is newer than that.

export const HEADER_LABEL = "CKA Practice: Storage & Advanced Troubleshooting";

export const PAPER = {
  slug: "cka-storage-advanced-troubleshooting",
  title: "CKA Practice: Storage & Advanced Troubleshooting",
  examType: "kubernetes",
  focusTags: ["CKA", "Storage", "PersistentVolumes", "Troubleshooting"],
  questionCount: 38,
  difficulty: "advanced",
  sortOrder: 5,
  timeLimitMinutes: 60,
  positiveMarks: 1,
  negativeMarks: 0,
};

const SRC_STORAGE =
  "Storage — authored from kubernetes.io documentation (Persistent Volumes, Storage Classes, Volume concepts) and the current CNCF CKA exam curriculum's Storage domain; verified via web search against current Kubernetes docs, not copied from any official CNCF exam material.";
const SRC_TROUBLESHOOTING =
  "Troubleshooting — authored from kubernetes.io documentation (Debug Pods/Nodes/Cluster, kubeadm cluster administration) and the current CNCF CKA exam curriculum's Troubleshooting domain; verified via web search, not copied from any official CNCF exam material.";

export const SECTIONS = [
  {
    name: "Storage: Volumes, Claims & Access Modes",
    subject: "Storage",
    sourceNote: SRC_STORAGE,
    questions: [
      { topic: "PV Access Modes", stem: "Which PersistentVolume access mode allows the volume to be mounted read-write by multiple Pods across multiple nodes simultaneously?", options: ["ReadWriteOnce", "ReadOnlyMany", "ReadWriteMany", "ReadWriteOncePod"], correct: 2, explanation: "ReadWriteMany (RWX) permits read-write mounting by many Pods on many nodes at once; this requires a volume backend that actually supports concurrent multi-node writes (e.g. NFS-backed or certain CSI drivers)." },
      { topic: "ReadWriteOncePod", stem: "How does the ReadWriteOncePod access mode differ from ReadWriteOnce?", options: ["ReadWriteOncePod restricts the volume to a single Pod cluster-wide, whereas ReadWriteOnce restricts it to a single node (multiple Pods on that node can still mount it)", "ReadWriteOncePod allows multiple nodes, ReadWriteOnce allows only one", "They are functionally identical; ReadWriteOncePod is just a newer alias for ReadWriteOnce", "ReadWriteOncePod only applies to Block volumes, ReadWriteOnce only to Filesystem volumes"], correct: 0, explanation: "ReadWriteOnce (RWO) restricts mounting to a single node, but multiple Pods co-located on that node could still mount it. ReadWriteOncePod (GA since Kubernetes v1.29, CSI-only) tightens this to exactly one Pod cluster-wide." },
      { topic: "volumeMode", stem: "Setting a PersistentVolume's volumeMode to 'Block' instead of the default 'Filesystem' means:", options: ["The volume is presented to the container as a raw block device with no filesystem, and the application must manage it directly", "The volume is automatically formatted with ext4 before mounting", "The volume can no longer be dynamically provisioned", "The volume becomes read-only"], correct: 0, explanation: "volumeMode: Block skips the filesystem layer entirely, exposing a raw block device to the container — used by applications (e.g. some databases) that manage their own storage layout." },
      { topic: "Static Provisioning", stem: "When statically provisioning storage, which fields must an administrator set directly on the PersistentVolume object?", options: ["capacity, accessModes, and a volume source (e.g. hostPath, nfs, or a CSI block) — the PVC is created separately to bind to it", "Only a StorageClass name; capacity is inferred from the PVC automatically", "Only accessModes; capacity is optional for statically provisioned volumes", "None — static provisioning requires a CSI driver and cannot be done manually"], correct: 0, explanation: "Static provisioning means an admin creates the PV directly with its capacity, accessModes, and a concrete volume source; a separately-created PVC then binds to it based on matching criteria." },
      { topic: "Reclaim Policy", stem: "What is the default persistentVolumeReclaimPolicy for a PersistentVolume created via dynamic provisioning?", options: ["Retain", "Delete", "Recycle", "Immediate"], correct: 1, explanation: "Dynamically-provisioned PVs default to reclaimPolicy: Delete — when the bound PVC is deleted, the PV and its underlying storage are deleted too, unless the StorageClass or PV was configured otherwise." },
      { topic: "Reclaim Policy — Retain", stem: "With reclaimPolicy: Retain, what happens to a PersistentVolume after its bound PersistentVolumeClaim is deleted?", options: ["The PV is automatically deleted along with its underlying storage", "The PV moves to the Released phase; its data and underlying storage are preserved, but it isn't automatically available for a new claim until manually reclaimed", "The PV is automatically recycled by wiping its data and returning to Available", "The PV remains Bound indefinitely, even with no matching PVC"], correct: 1, explanation: "Retain preserves the PV's data for manual recovery. The PV transitions to Released — its data is intact, but Kubernetes won't automatically rebind it to a new PVC." },
      { topic: "Reusing a Released PV", stem: "A Retain-policy PV is stuck in the Released phase after its PVC was deleted. To make it bindable again for a new PVC, what does an admin typically need to do?", options: ["Nothing — Kubernetes automatically rebinds Released PVs to the next matching PVC", "Manually clear the PV's spec.claimRef field (and typically also confirm/clean the underlying data) before it can be bound again", "Delete and recreate the StorageClass", "Set the PV's accessModes to ReadWriteMany"], correct: 1, explanation: "A Released PV retains a stale claimRef pointing at the deleted PVC. An admin must manually edit the PV to clear claimRef (and handle the retained data appropriately) before it becomes Available again." },
      { topic: "PV-PVC Binding", stem: "For the control plane to bind a PersistentVolumeClaim to a specific pre-existing PersistentVolume, which of the following must hold?", options: ["The PV's capacity must be >= the PVC's request, its accessModes must include what the PVC requests, and its storageClassName must match (an empty PVC class matches only classless PVs, unless a default class applies)", "Only that the PV and PVC share the same name", "Only that the PV's capacity exactly equals the PVC's request, down to the byte", "PVCs cannot bind to pre-existing PVs at all — only to dynamically provisioned ones"], correct: 0, explanation: "Binding requires sufficient capacity, compatible access modes, and a matching storageClassName (plus any selector/label requirements) — not an exact capacity match or matching names." },
      { topic: "Multi-Attach Error", stem: "A Pod fails to start with an event reading: 'Multi-Attach error for volume ... Volume is already exclusively attached to one node and can't be attached to another'. What is the most likely cause?", options: ["The underlying PV uses an access mode like ReadWriteOnce, and a Pod using the same PVC is still (or was recently) attached on a different node — common during a reschedule", "The StorageClass has allowVolumeExpansion set to false", "The Pod's securityContext.runAsNonRoot is set to true", "The NetworkPolicy for the namespace is blocking the CSI driver's traffic"], correct: 0, explanation: "RWO volumes can only be attached to one node at a time. This error commonly appears during a Pod reschedule where the old node hasn't released the attachment yet." },
      { topic: "subPath", stem: "What does specifying subPath in a container's volumeMount allow you to do?", options: ["Mount only a specific file or subdirectory from within the referenced volume, rather than the volume's entire root", "Mount the same volume as read-only automatically", "Split a single PVC into multiple independent PVs", "Bypass the access mode restrictions of the underlying PV"], correct: 0, explanation: "subPath lets a container mount a sub-path inside a volume — useful for sharing a single volume across containers where each needs only a specific file or directory." },
      { topic: "Generic Ephemeral Volumes", stem: "How does a generic ephemeral volume (a Pod's volumes[].ephemeral field) differ from a plain emptyDir volume?", options: ["It's backed by a PersistentVolumeClaim that's automatically created and tied to the Pod's lifecycle, so it can use any CSI storage driver/StorageClass — unlike emptyDir, which is always local node storage", "It's identical to emptyDir but with a different name in the API", "It can only be used with StatefulSets, never with Deployments", "It persists even after the Pod is permanently deleted, unlike a PVC-backed volume"], correct: 0, explanation: "Generic ephemeral volumes get a real, auto-created PVC (deleted with the Pod), letting Pod-lifecycle-scoped storage use any StorageClass/CSI driver, unlike emptyDir's plain node-local storage." },
      { topic: "StatefulSet volumeClaimTemplates", stem: "When a StatefulSet defines volumeClaimTemplates, what happens to the PVCs it creates when the StatefulSet is scaled down, if persistentVolumeClaimRetentionPolicy isn't set to Delete for whenScaled?", options: ["The PVCs are deleted immediately along with the terminated Pods", "The PVCs are retained by default, so scaling back up reattaches the same PVC (and its data) to the recreated ordinal Pod", "The PVCs are automatically converted to a different StorageClass", "The PVCs become read-only until manually released"], correct: 1, explanation: "By default (or with whenScaled: Retain), volumeClaimTemplates-created PVCs survive scale-down, preserving per-ordinal data for when that replica is recreated. persistentVolumeClaimRetentionPolicy (stable since v1.32) lets you opt into Delete instead." },
    ],
  },
  {
    name: "Storage: Dynamic Provisioning & StorageClasses",
    subject: "Storage",
    sourceNote: SRC_STORAGE,
    questions: [
      { topic: "StorageClass API", stem: "Which API group/version defines the StorageClass resource?", options: ["storage.k8s.io/v1", "apps/v1", "core/v1 (no group)", "scheduling.k8s.io/v1"], correct: 0, explanation: "StorageClass is defined under the storage.k8s.io/v1 API group, alongside related storage resources like VolumeAttachment and CSIDriver." },
      { topic: "volumeBindingMode", stem: "Setting a StorageClass's volumeBindingMode to WaitForFirstConsumer instead of the default Immediate means:", options: ["PV provisioning/binding is delayed until a Pod using the PVC is actually scheduled, so the volume can be created in the same zone/node topology as that Pod", "The PVC will never be bound to any PV", "Provisioning happens immediately, but the Pod is never scheduled until the PVC is bound", "It disables dynamic provisioning entirely, forcing static provisioning"], correct: 0, explanation: "WaitForFirstConsumer defers binding/provisioning until a consuming Pod exists, allowing topology-aware placement (e.g. provisioning in the same availability zone the Pod actually lands in)." },
      { topic: "allowVolumeExpansion", stem: "A StorageClass has allowVolumeExpansion: false. What happens if you edit a bound PVC's spec.resources.requests.storage to a larger value?", options: ["The expansion request is rejected/has no effect — allowVolumeExpansion must be true on the StorageClass for a PVC using it to be resized", "The PVC shrinks instead of growing", "The PV is deleted and recreated automatically at the new size", "The change succeeds regardless, since allowVolumeExpansion only affects reclaim behavior"], correct: 0, explanation: "Volume expansion is gated by the StorageClass's allowVolumeExpansion field; without it set to true, edits to a PVC's requested size don't trigger resizing." },
      { topic: "Volume Expansion Direction", stem: "For a StorageClass that does support volume expansion, which resize direction is supported by editing the PVC's requested storage size?", options: ["Only growing the volume to a larger size — shrinking a PVC's storage request is not supported", "Only shrinking, never growing", "Both growing and shrinking are fully supported", "Neither — a new PVC must always be created"], correct: 0, explanation: "Kubernetes volume expansion only supports increasing size; there is no supported mechanism to shrink a PVC's storage request in place." },
      { topic: "Default StorageClass", stem: "A cluster has a StorageClass annotated storageclass.kubernetes.io/is-default-class: 'true'. What happens when a PVC is created without specifying storageClassName?", options: ["The PVC stays Pending forever, since storageClassName is mandatory", "The PVC is automatically assigned that default StorageClass, and dynamic provisioning proceeds via its provisioner", "The PVC binds only to statically-provisioned PVs with no class at all", "Kubernetes rejects the PVC at admission"], correct: 1, explanation: "An admission controller applies the annotated default StorageClass to any PVC that omits storageClassName, so provisioning proceeds normally through that class's provisioner." },
      { topic: "PVC with No Default StorageClass", stem: "A PVC is created with storageClassName left unset, and the cluster has NO StorageClass marked as default. What happens?", options: ["It's treated the same as explicitly setting storageClassName: '' and will only bind to a pre-existing PV that also has no storage class — otherwise it stays Pending", "It automatically picks whichever StorageClass was created first", "It fails validation and cannot be created", "It provisions using the CSI driver with the fewest existing volumes"], correct: 0, explanation: "With no default StorageClass, an unset storageClassName behaves like an empty string — the PVC can only bind to a classless, statically-provisioned PV, and otherwise remains Pending indefinitely." },
      { topic: "Provisioner Field", stem: "In a StorageClass definition, what does the provisioner field specify?", options: ["The volume plugin (in-tree name or, more commonly today, a CSI driver name) responsible for actually creating the underlying storage on demand", "The Kubernetes node designated to host all PVs from this class", "The maximum number of PVCs allowed to use this class", "The RBAC role permitted to create PVCs against this class"], correct: 0, explanation: "provisioner names the plugin (typically a CSI driver such as ebs.csi.aws.com) that dynamically creates storage when a PVC requests this class." },
      { topic: "CSI Drivers", stem: "What is the primary role of a CSI (Container Storage Interface) driver in modern Kubernetes storage?", options: ["It implements a standardized plugin interface so storage vendors can provide provisioning/attaching/mounting logic without that code living in Kubernetes core (replacing older in-tree volume plugins)", "It replaces kube-proxy for storage-related networking", "It's only used for logging PV events, with actual provisioning still handled in-tree", "It's required only for emptyDir and hostPath volumes"], correct: 0, explanation: "CSI decouples storage-vendor logic from Kubernetes core, letting new storage backends be supported as out-of-tree drivers rather than requiring changes to Kubernetes itself." },
      { topic: "PVC Immutable Fields", stem: "Which of the following PVC spec fields is generally immutable after creation (cannot be edited on an existing PVC)?", options: ["accessModes and volumeMode — the requested storage size can be increased if the StorageClass allows expansion, but access mode/volume mode cannot be changed after the fact", "resources.requests.storage, which can never be changed under any circumstance", "storageClassName, which can always be freely changed after binding", "None of the PVC spec fields are immutable"], correct: 0, explanation: "A PVC's accessModes and volumeMode are fixed at creation; only resources.requests.storage can be changed afterward (and only to increase it, if the StorageClass permits expansion)." },
      { topic: "Local PersistentVolumes", stem: "A 'local' PersistentVolume (using volume source local:) ties storage to a specific node's disk. What must typically accompany it so Pods are only scheduled to that node?", options: ["A nodeAffinity rule on the PV itself, since local storage isn't network-attached and can't just be mounted from any node", "A NetworkPolicy restricting traffic to that node", "Nothing extra — local PVs are automatically node-agnostic", "A DaemonSet must be deployed to replicate the data to every node"], correct: 0, explanation: "Because local volumes only exist on one node's disk, the PV must declare a nodeAffinity so the scheduler places consuming Pods on that exact node." },
    ],
  },
  {
    name: "Advanced Troubleshooting",
    subject: "Troubleshooting",
    sourceNote: SRC_TROUBLESHOOTING,
    questions: [
      { topic: "PVC Stuck Pending", stem: "A PVC has been stuck in Pending state for several minutes. Which is the most useful first diagnostic command?", options: ["kubectl describe pvc <name> — its Events typically show the exact reason (no matching PV, no default StorageClass, provisioner failure, etc.)", "kubectl logs <pvc-name>", "kubectl rollout status pvc/<name>", "kubectl top pvc <name>"], correct: 0, explanation: "PVCs, like Pods, surface Events explaining scheduling/binding failures via kubectl describe — this is the fastest path to the actual root cause." },
      { topic: "OOMKilled", stem: "A container's status shows 'Last State: Terminated, Reason: OOMKilled'. What does this specifically mean?", options: ["The container exceeded its memory limit (or the node ran out of available memory) and the kernel's OOM killer terminated its process", "The container was killed because its livenessProbe failed", "The container exceeded its CPU limit and was throttled", "The container failed to pull its image within a timeout"], correct: 0, explanation: "OOMKilled indicates the Linux out-of-memory killer terminated the process, typically because the container hit its memory limit or the node itself was under memory pressure." },
      { topic: "Node Memory Pressure Eviction", stem: "Under node-level MemoryPressure, when the kubelet needs to evict Pods to reclaim resources, which Pods does it generally evict first?", options: ["BestEffort Pods (no requests/limits set) first, then Burstable Pods exceeding their requests, with Guaranteed Pods evicted last", "Guaranteed QoS Pods first, to protect BestEffort workloads", "Whichever Pod has been running longest, regardless of QoS class", "Only Pods in the kube-system namespace"], correct: 0, explanation: "Kubelet eviction ordering follows QoS class: BestEffort Pods are evicted first, then Burstable Pods using more than their requests, and Guaranteed Pods last." },
      { topic: "Insufficient CPU Scheduling Failure", stem: "A Pod stays Pending, and 'kubectl describe pod' shows a FailedScheduling event: '0/3 nodes are available: 3 Insufficient cpu.' What does this indicate?", options: ["No node currently has enough allocatable CPU left to satisfy the Pod's CPU requests, given what's already requested by other Pods on each node", "The Pod's image failed to pull on all 3 nodes", "The cluster's CPU manager policy is set to 'none'", "A NetworkPolicy is blocking the scheduler from reaching any node"], correct: 0, explanation: "This message means the scheduler evaluated all 3 nodes and found none with enough unreserved CPU capacity to satisfy the Pod's resources.requests.cpu." },
      { topic: "Node Affinity Scheduling Failure", stem: "A Pod requires a node affinity rule with requiredDuringSchedulingIgnoredDuringExecution matching a label no node currently has. What happens?", options: ["The Pod remains Pending indefinitely with a FailedScheduling event, since a 'required' rule is a hard constraint", "The scheduler ignores the rule after 30 seconds and schedules it anyway", "The Pod is scheduled to a random node and the label is applied automatically", "The API server rejects the Pod at admission time"], correct: 0, explanation: "requiredDuringSchedulingIgnoredDuringExecution is a hard constraint; if no node satisfies it, the Pod simply cannot be scheduled and stays Pending." },
      { topic: "Taint Without Toleration", stem: "A node has a taint key=value:NoSchedule, and a Pod has no matching toleration. What's the effect on that Pod's scheduling to that node?", options: ["The scheduler will not place the Pod on that node at all, though already-running Pods on it (scheduled before the taint) are unaffected by NoSchedule specifically", "The Pod is scheduled there anyway but immediately evicted", "The taint only affects Services, not Pod scheduling", "The Pod is scheduled there with a warning event but otherwise runs normally"], correct: 0, explanation: "NoSchedule prevents new Pods without a matching toleration from being scheduled to the tainted node; it does not evict existing Pods (that's NoExecute's behavior)." },
      { topic: "ImagePullBackOff Root Causes", stem: "Which of the following is NOT a typical root cause of a Pod stuck in ImagePullBackOff?", options: ["A typo in the image name or tag", "Missing imagePullSecrets for a private registry", "Registry rate-limiting, or the registry being unreachable from the node", "The Pod's resources.limits.memory being set too low"], correct: 3, explanation: "Memory limits affect runtime OOM behavior, not image pulling. Typos, missing pull secrets, and registry unavailability/rate-limiting are the classic ImagePullBackOff causes." },
      { topic: "ErrImagePull vs ImagePullBackOff", stem: "What's the relationship between the ErrImagePull and ImagePullBackOff states seen in kubectl get pods?", options: ["ErrImagePull is the immediate failed pull attempt; ImagePullBackOff is the kubelet backing off with increasing delay between repeated retries after ErrImagePull recurs", "They are unrelated failure states from entirely different subsystems", "ImagePullBackOff always precedes ErrImagePull, never the other way around", "ErrImagePull only occurs for public images, ImagePullBackOff only for private ones"], correct: 0, explanation: "ErrImagePull reports an individual failed pull; if it keeps failing, the kubelet enters an exponential backoff loop shown as ImagePullBackOff." },
      { topic: "Structured Troubleshooting", stem: "When kubectl describe pod's Events section doesn't give enough detail about a scheduling or runtime failure, what's the most direct way to inspect the full resource state as Kubernetes stored it?", options: ["kubectl get pod <name> -o yaml (or -o json), to see the complete spec/status as recorded, including fields describe may summarize or omit", "kubectl top pod <name>", "kubectl config view", "kubectl api-resources"], correct: 0, explanation: "-o yaml/-o json dumps the raw object, including status fields and conditions that describe's human-readable summary may compress or leave out." },
      { topic: "Anti-Affinity Scheduling Failure", stem: "A Deployment's Pods use podAntiAffinity with requiredDuringSchedulingIgnoredDuringExecution to avoid co-locating replicas on the same node. What happens when you scale the Deployment beyond the number of available nodes?", options: ["The excess Pods remain Pending, since the hard anti-affinity rule can't be satisfied once every node already has one matching Pod", "Multiple replicas are automatically allowed on the same node once nodes run out", "The anti-affinity rule is silently dropped for the excess Pods", "The Deployment controller automatically provisions new nodes"], correct: 0, explanation: "A 'required' anti-affinity rule is a hard constraint; once every node hosts one matching Pod, no node remains valid for further replicas, so they stay Pending." },
      { topic: "etcd Health Troubleshooting", stem: "You suspect etcd is unhealthy and it's causing slow kube-apiserver responses cluster-wide. Which command directly checks etcd's own health/latency?", options: ["etcdctl endpoint health (and endpoint status), run against the etcd endpoints with the appropriate TLS cert/key/CA flags", "kubectl get componentstatuses, which fully replaces the need for etcdctl in current Kubernetes versions", "kubectl describe node, which reports etcd latency directly", "kubectl logs etcd -n default"], correct: 0, explanation: "etcdctl talks to etcd directly and reports endpoint health/latency; kubectl-level commands don't give this level of etcd-internal detail." },
      { topic: "CoreDNS Troubleshooting", stem: "Pods across the cluster suddenly can't resolve any Service DNS names, though they could earlier. Beyond checking the Pod's own resolv.conf, what's a key cluster-level thing to check?", options: ["Whether the CoreDNS Pods (in kube-system) are actually Running/Ready, and check their logs for errors — a CoreDNS outage breaks cluster-wide DNS for every Pod", "Whether the Pod's own securityContext.runAsUser is set correctly", "Whether the PersistentVolume backing the Pod is Bound", "Whether the Deployment's revisionHistoryLimit is set too low"], correct: 0, explanation: "A cluster-wide DNS outage points at the CoreDNS Deployment itself — check its Pods' status and logs before assuming a per-Pod configuration issue." },
      { topic: "Service Not Routing Traffic", stem: "A Service exists and its selector matches Pod labels correctly, but clients still can't reach it. What's a good next diagnostic step, beyond checking the Service's Endpoints/EndpointSlice?", options: ["Confirm the target Pods are actually Ready (failing readinessProbe Pods are excluded from Endpoints even if labels match), and check that kube-proxy is running correctly on the relevant nodes", "Delete and recreate the Namespace", "Increase the Service's port number", "Switch the Service's type from ClusterIP to Headless, which always fixes routing issues"], correct: 0, explanation: "Matching labels alone doesn't guarantee traffic flows — readiness gates Endpoints membership, and kube-proxy must be functioning to actually program the routing rules." },
      { topic: "kubelet Node-Level Logs", stem: "On a kubeadm-provisioned node running a systemd-managed kubelet, which command shows the kubelet's own logs for deep node-level troubleshooting (not container logs)?", options: ["journalctl -u kubelet", "kubectl logs kubelet -n kube-system", "crictl logs kubelet", "kubectl describe node --logs"], correct: 0, explanation: "The kubelet itself typically runs as a systemd unit, not a Pod, so its logs are accessed via journalctl -u kubelet on the node, not via kubectl logs." },
      { topic: "crictl for Runtime-Level Debugging", stem: "kubectl-level commands aren't giving enough detail about why a container keeps failing on a specific node. What's crictl used for in this situation?", options: ["It talks directly to the node's container runtime (via the CRI) to inspect/debug containers, images, and pods at a level below what kubectl exposes", "It's a kubectl plugin for cross-cluster comparison", "It replaces kubeadm for cluster bootstrapping", "It's only used to inspect etcd's raft log"], correct: 0, explanation: "crictl is a CLI for the Container Runtime Interface, letting you inspect containers/images/pods directly on a node's runtime (containerd, CRI-O) independent of the API server." },
      { topic: "Certificate Expiry Failures", stem: "kubectl commands suddenly start failing cluster-wide with an error like 'x509: certificate has expired or is not yet valid', and no recent config changes were made. What's a likely cause worth checking first?", options: ["The kubeadm-managed control plane certificates have expired (kubeadm certs expire after 1 year by default unless renewed), or there's significant clock/time skew between nodes", "The Pod's imagePullPolicy is set to IfNotPresent", "A PersistentVolume's reclaimPolicy was changed", "The cluster's default StorageClass was deleted"], correct: 0, explanation: "kubeadm-generated certificates have a 1-year default validity; expiry (or major clock skew, since x509 validation is time-based) is the classic cause of this exact error appearing cluster-wide." },
    ],
  },
];
